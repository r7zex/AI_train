import type { FlowTopic } from '../aiCurriculumTypes'
import { code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from './helpers'

const topicNumpyWhy = numpyTopic(
  'numpy-why',
  '2.1 Зачем нужен NumPy',
  1,
  'Объясняем роль NumPy в ML и отличие ndarray от обычного списка Python.',
  'NumPy нужен для быстрых операций над числовыми массивами, на которых строятся pandas, sklearn и scipy.',
  ['NumPy', 'ndarray', 'массив', 'векторизация'],
  ['объекты x признаки -> числовая матрица -> модель'],
  ['В ML табличные данные часто становятся матрицей; NumPy даёт быстрые операции над такими матрицами.'],
  [
    theoryStep(
      'numpy-why-ml',
      'Зачем NumPy нужен в ML',
      'В ML данные часто превращаются в числовую матрицу.',
      [
        section('matrix', 'Числовая матрица для модели', [
          'В ML данные часто превращаются в числовую таблицу: строки - объекты, столбцы - признаки.',
          'Модель обычно не работает напрямую с сырым текстом “двухкомнатная квартира рядом с метро”. Сначала данные кодируют числами, а затем передают в алгоритм как массив.',
        ], {
          codeExamples: [
            code('text', 'объекты x признаки -> числовая матрица -> модель'),
          ],
        }),
        section('why', 'Почему именно NumPy', [
          'NumPy нужен для быстрых операций над такими числовыми массивами. Он лежит в основе pandas, sklearn, scipy и многих других инструментов Python-экосистемы.',
          'Даже если вы часто пишете код через pandas или sklearn, под капотом данные и операции всё равно часто переходят в NumPy-массивы.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-why-ndarray',
      'ndarray',
      '`ndarray` - основной объект NumPy.',
      [
        section('object', '`ndarray`', [
          '`ndarray` - основной объект NumPy. Это n-мерный массив, где элементы обычно имеют один тип данных.',
          'Массив хранит данные компактно, знает свою форму, поддерживает быстрые операции и может быть одномерным, двумерным или многомерным.',
        ], {
          bullets: ['хранит данные компактно', 'знает свою форму', 'поддерживает быстрые операции', 'может быть одномерным, двумерным и многомерным'],
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30])
              print(type(x))
              print(x)
            `),
          ],
        }),
        section('ml', 'Где встречается в ML', [
          'Одномерный `ndarray` часто используют как вектор значений или target. Двумерный `ndarray` - как матрицу объектов и признаков.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-why-list-vs-array',
      'Список Python vs NumPy-массив',
      'Python list и NumPy array ведут себя по-разному.',
      [
        section('comparison', 'Чем массив отличается от списка', [
          'Python list удобен для обычных структур данных, но численные операции над большими списками часто приходится писать через цикл.',
          'NumPy-массив обычно хранит элементы одного типа и умеет применять операции сразу ко всему массиву.',
        ], {
          table: {
            headers: ['Python list', 'NumPy array'],
            rows: [
              ['Может хранить разные типы', 'Обычно один тип'],
              ['Операции часто через цикл', 'Операции сразу над массивом'],
              ['Удобен для обычных задач', 'Удобен для численных вычислений'],
              ['Медленнее на больших числовых массивах', 'Быстрее на больших числовых массивах'],
            ],
          },
          codeExamples: [
            code('python', `
              import numpy as np

              a = [1, 2, 3]
              x = np.array([1, 2, 3])

              print(a + a)
              print(x + x)
            `),
          ],
        }),
        section('behavior', 'Главное отличие в примере', [
          'Список при `+` склеивается, NumPy-массив складывается поэлементно. Это первая важная разница, которую нужно запомнить.',
        ]),
      ],
    ),
    quizStep(
      'numpy-why-quiz-ndarray',
      'Что такое ndarray',
      'Проверяем главный объект NumPy.',
      singleQuiz(
        'quiz-numpy-why-ndarray',
        'ndarray',
        'numpy-why',
        'numpy-ml',
        'Что такое `ndarray` в NumPy?',
        [
          { id: 'a', text: 'Тип графика' },
          { id: 'b', text: 'Основной объект массива в NumPy' },
          { id: 'c', text: 'Метод pandas' },
          { id: 'd', text: 'Функция для обучения модели' },
        ],
        'b',
        '`ndarray` - основной объект NumPy для хранения n-мерных массивов.',
      ),
    ),
    quizStep(
      'numpy-why-quiz-ml',
      'Почему NumPy удобен',
      'Проверяем роль NumPy в ML.',
      singleQuiz(
        'quiz-numpy-why-ml',
        'Зачем NumPy',
        'numpy-why',
        'numpy-ml',
        'Почему NumPy часто используют в ML?',
        [
          { id: 'a', text: 'Он ускоряет численные операции над массивами' },
          { id: 'b', text: 'Он сам обучает любые модели без данных' },
          { id: 'c', text: 'Он заменяет Python полностью' },
          { id: 'd', text: 'Он нужен только для текста' },
        ],
        'a',
        'NumPy удобен для быстрых численных операций над массивами, которые часто лежат в основе ML-данных.',
      ),
    ),
    practiceStep(
      'numpy-why-practice',
      'Первый массив',
      'Создаём массив и выводим его размер.',
      makeStdinTask(
        'task-numpy-first-array',
        'Первый массив',
        'На вход подаются числа через пробел. Создайте NumPy-массив и выведите его размер.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте NumPy-массив

          # TODO: выведите количество элементов
        `,
        [
          { id: 's1', description: 'Четыре числа', input: '1 2 3 4', expectedOutput: '4' },
        ],
        [
          { id: 'h1', description: 'Два числа', input: '10 20', expectedOutput: '2' },
          { id: 'h2', description: 'Одно число', input: '5', expectedOutput: '1' },
        ],
        `
          import numpy as np

          numbers = list(map(float, input().split()))
          x = np.array(numbers)
          print(x.size)
        `,
      ),
    ),
  ],
)

const topicNumpyCreation = numpyTopic(
  'numpy-array-creation',
  '2.2 Создание массивов',
  2,
  'Изучаем `np.array()`, заготовки из одинаковых значений, `np.arange()` и `np.linspace()`.',
  'Разные функции создания массивов нужны для разных стартовых ситуаций: готовые данные, заготовка, диапазон или сетка.',
  ['np.array', 'np.zeros', 'np.ones', 'np.full', 'np.arange', 'np.linspace'],
  ['np.arange(start, stop, step)', 'np.linspace(start, stop, num)'],
  ['`np.array()` создаёт массив из данных; `zeros/ones/full` делают заготовки; `arange/linspace` создают последовательности.'],
  [
    theoryStep(
      'numpy-array-creation-array',
      '`np.array()`',
      '`np.array()` создаёт массив из готовых данных.',
      [
        section('purpose', 'Назначение', [
          '`np.array(object, dtype=None)` создаёт массив из готовых данных: списка, списка списков или другой структуры.',
          'Параметр `object` содержит исходные данные. Параметр `dtype` задаёт тип данных явно, если это нужно для стабильности вычислений.',
          'Возвращает `ndarray`: массив NumPy, над которым дальше можно выполнять векторные операции.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])
              print(x)
            `, '[1 2 3]'),
          ],
        }),
        section('ml', 'Где используется в ML', [
          '`np.array()` применяют для создания вектора признаков, target, массива весов, небольшого батча данных или учебного примера.',
          'Возвращаемое значение - `ndarray`, то есть стандартный массив NumPy.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-array-creation-fillers',
      '`np.zeros()`, `np.ones()`, `np.full()`',
      'Похожие функции для массивов, заполненных одним значением.',
      [
        section('functions', 'Три функции-заготовки', [
          '`np.zeros(shape)` создаёт массив из нулей. `np.ones(shape)` создаёт массив из единиц. `np.full(shape, fill_value)` создаёт массив, заполненный одним значением.',
          'Эти функции удобно рассматривать вместе: они отличаются только значением, которым заполнен массив.',
          'Параметр `shape` задаёт форму результата, а `fill_value` в `np.full()` задаёт значение для заполнения. Каждая функция возвращает `ndarray` указанной формы.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              print(np.zeros(3))
              print(np.ones(3))
              print(np.full(3, 7))
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Такие массивы используют как начальные веса, счётчики, маски и заготовки массивов нужной формы.',
          'Для двумерной формы можно передать tuple, например `np.zeros((2, 3))` создаст таблицу из 2 строк и 3 столбцов.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-array-creation-arange',
      '`np.arange()`',
      '`np.arange()` создаёт последовательность с шагом.',
      [
        section('syntax', 'Синтаксис и поведение', [
          '`np.arange(start, stop, step)` создаёт последовательность чисел с шагом. Важная деталь: `stop` не включается.',
          'Если передать один аргумент, NumPy считает его `stop`, а `start` берёт равным 0. Поэтому `np.arange(5)` даёт числа от 0 до 4.',
          'Возвращает одномерный `ndarray`. Параметр `step` может быть отрицательным, если нужна последовательность в обратном направлении.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.arange(0, 10, 2)
              print(x)
            `, '[0 2 4 6 8]'),
          ],
        }),
        section('ml', 'Где используется', [
          '`np.arange()` удобно применять для индексов, простых диапазонов, номеров объектов и быстрых учебных массивов.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-array-creation-linspace',
      '`np.linspace()`',
      '`np.linspace()` создаёт равномерную сетку значений.',
      [
        section('syntax', 'Синтаксис и поведение', [
          '`np.linspace(start, stop, num)` создаёт `num` равномерно расположенных чисел от `start` до `stop`.',
          'В отличие от `np.arange()`, конечное значение `stop` по умолчанию включается. Это удобно, когда нужно получить ровно заданное количество точек.',
          'Параметр `num` задаёт количество точек, а возвращаемое значение - одномерный `ndarray` с вещественными числами.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.linspace(0, 1, 5)
              print(x)
            `, '[0.   0.25 0.5  0.75 1.  ]'),
          ],
        }),
        section('ml', 'Где используется', [
          '`np.linspace()` часто используют для сетки значений на графиках, экспериментов с параметрами и визуализации функций.',
        ]),
      ],
    ),
    quizStep(
      'numpy-array-creation-quiz-function',
      'Выбрать функцию создания',
      'Проверяем выбор `linspace`.',
      singleQuiz(
        'quiz-numpy-array-creation-function',
        'Функция создания',
        'numpy-array-creation',
        'numpy-ml',
        'Какую функцию лучше использовать, чтобы создать 5 чисел от 0 до 1 включительно с равным шагом?',
        [
          { id: 'a', text: '`np.zeros(5)`' },
          { id: 'b', text: '`np.arange(5)`' },
          { id: 'c', text: '`np.linspace(0, 1, 5)`' },
          { id: 'd', text: '`np.full(5, 1)`' },
        ],
        'c',
        '`np.linspace(0, 1, 5)` создаёт ровно 5 равномерных точек от 0 до 1 включительно.',
      ),
    ),
    quizStep(
      'numpy-array-creation-quiz-arange',
      'Что получится на выходе',
      'Проверяем, что `stop` в `arange` не включается.',
      singleQuiz(
        'quiz-numpy-array-creation-arange',
        'Результат arange',
        'numpy-array-creation',
        'numpy-ml',
        'Что вернёт `np.arange(0, 6, 2)`?',
        [
          { id: 'a', text: '`[0, 2, 4]`' },
          { id: 'b', text: '`[0, 2, 4, 6]`' },
          { id: 'c', text: '`[2, 4, 6]`' },
          { id: 'd', text: '`[0, 1, 2, 3, 4, 5, 6]`' },
        ],
        'a',
        '`np.arange()` не включает `stop`, поэтому значение 6 не попадает в массив.',
      ),
    ),
    practiceStep(
      'numpy-array-creation-practice',
      'Создать массивы',
      'Практика на функции создания массивов.',
      makeStdinTask(
        'task-numpy-create-arrays',
        'Создать массивы',
        'На вход подаются `n` и `k`. Создайте массив из нулей длины `n`, массив чисел от `0` до `n - 1`, массив из `k` равномерных точек от `0` до `1`. Выведите каждый массив с новой строки.',
        `
          import numpy as np

          # TODO: считайте n и k

          # TODO: создайте массив из нулей длины n

          # TODO: создайте массив от 0 до n - 1

          # TODO: создайте k точек от 0 до 1

          # TODO: выведите результаты
        `,
        [
          { id: 's1', description: 'n=4, k=3', input: '4 3', expectedOutput: '[0. 0. 0. 0.]\n[0 1 2 3]\n[0.  0.5 1. ]' },
        ],
        [
          { id: 'h1', description: 'n=3, k=5', input: '3 5', expectedOutput: '[0. 0. 0.]\n[0 1 2]\n[0.   0.25 0.5  0.75 1.  ]' },
          { id: 'h2', description: 'n=2, k=2', input: '2 2', expectedOutput: '[0. 0.]\n[0 1]\n[0. 1.]' },
        ],
        `
          import numpy as np

          n, k = map(int, input().split())
          zeros = np.zeros(n)
          sequence = np.arange(n)
          points = np.linspace(0, 1, k)
          print(zeros)
          print(sequence)
          print(points)
        `,
      ),
    ),
  ],
)

const topicNumpyShape = numpyTopic(
  'numpy-shape-ndim-dtype',
  '2.3 Форма, размерность и тип данных',
  3,
  'Разбираем свойства массива `shape`, `ndim`, `dtype` и их роль в ML.',
  '`shape` показывает форму, `ndim` - число измерений, `dtype` - тип элементов.',
  ['shape', 'ndim', 'dtype', 'n_samples', 'n_features'],
  ['X.shape = (n_samples, n_features)'],
  ['Для sklearn матрица признаков обычно имеет форму `(n_samples, n_features)`.'],
  [
    theoryStep(
      'numpy-shape-shape',
      '`shape`',
      '`shape` показывает форму массива.',
      [
        section('definition', 'Что показывает shape', [
          '`shape` показывает форму массива: сколько элементов лежит вдоль каждой оси.',
          'Для двумерного массива `(2, 3)` означает 2 строки и 3 столбца. В ML это можно читать как 2 объекта и 3 признака.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([[1, 2, 3],
                            [4, 5, 6]])

              print(x.shape)
            `, '(2, 3)'),
          ],
        }),
        section('ml', 'Где используется', [
          'Форма массива помогает быстро понять, сколько объектов и признаков лежит в данных. Ошибка формы - одна из самых частых причин проблем при обучении модели.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-shape-ndim',
      '`ndim`',
      '`ndim` показывает число измерений массива.',
      [
        section('definition', 'Что показывает ndim', [
          '`ndim` показывает число измерений массива. Одномерный массив похож на вектор, двумерный - на таблицу или матрицу.',
          'Трёхмерные и более высокие размерности встречаются, например, в батчах изображений, последовательностях или наборах матриц.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])
              X = np.array([[1, 2, 3], [4, 5, 6]])

              print(x.ndim)
              print(X.ndim)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Многие функции ожидают конкретное число измерений. Например, модель может ожидать двумерный `X`, даже если у объекта всего один признак.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-shape-dtype',
      '`dtype`',
      '`dtype` показывает тип данных элементов массива.',
      [
        section('definition', 'Что показывает dtype', [
          '`dtype` показывает тип данных внутри массива: например `int64`, `float64`, `bool` или строковый тип.',
          'Обычно NumPy старается привести элементы массива к одному типу. Если смешать целые и вещественные числа, результат часто станет вещественным массивом.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])
              y = np.array([1.0, 2.0, 3.0])

              print(x.dtype)
              print(y.dtype)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Для ML обычно нужны числовые типы. Строковые категории перед моделью кодируют, а булевы маски используют для фильтрации.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-shape-ml',
      'Почему форма важна в ML',
      'ML-модели ожидают матрицу объектов и признаков.',
      [
        section('shape-ml', 'Стандартная форма', [
          'ML-модели часто ожидают данные в форме `X.shape = (n_samples, n_features)`.',
          '`n_samples` - количество объектов, `n_features` - количество признаков. Например, 100 квартир и 3 признака дают `X.shape = (100, 3)`.',
        ]),
        section('errors', 'Что будет при неправильной форме', [
          'Если форма неправильная, модель может не обучиться или получить не те признаки. Иногда ошибка сразу видна по traceback, а иногда модель просто учится на неверно расположенных данных.',
          'Поэтому перед обучением полезно явно проверять `X.shape`, `y.shape`, `X.ndim` и типы данных.',
        ]),
      ],
    ),
    quizStep(
      'numpy-shape-quiz-shape',
      'Определить shape',
      'Проверяем форму двумерного массива.',
      singleQuiz(
        'quiz-numpy-shape',
        'Shape массива',
        'numpy-shape-ndim-dtype',
        'numpy-ml',
        'Какой `shape` у массива `np.array([[1, 2, 3], [4, 5, 6]])`?',
        [
          { id: 'a', text: '`(3, 2)`' },
          { id: 'b', text: '`(2, 3)`' },
          { id: 'c', text: '`(6,)`' },
          { id: 'd', text: '`(2,)`' },
        ],
        'b',
        'В массиве 2 строки и 3 столбца, поэтому форма равна `(2, 3)`.',
      ),
    ),
    quizStep(
      'numpy-shape-quiz-dtype',
      'Определить ndim и dtype',
      'Проверяем смысл `dtype`.',
      singleQuiz(
        'quiz-numpy-dtype',
        'Dtype массива',
        'numpy-shape-ndim-dtype',
        'numpy-ml',
        'Что показывает `dtype`?',
        [
          { id: 'a', text: 'Форму массива' },
          { id: 'b', text: 'Количество измерений' },
          { id: 'c', text: 'Тип данных элементов массива' },
          { id: 'd', text: 'Количество строк' },
        ],
        'c',
        '`dtype` показывает тип элементов, например `int64`, `float64` или `bool`.',
      ),
    ),
    practiceStep(
      'numpy-shape-practice',
      'Проверить свойства массива',
      'Выводим `shape`, `ndim` и `dtype`.',
      makeStdinTask(
        'task-numpy-array-properties',
        'Проверить свойства массива',
        'На вход подаются числа. Создайте массив с `dtype=float` и выведите: `shape`, `ndim`, `dtype`.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте массив

          # TODO: выведите shape

          # TODO: выведите ndim

          # TODO: выведите dtype
        `,
        [
          { id: 's1', description: 'Четыре числа', input: '1 2 3 4', expectedOutput: '(4,)\n1\nfloat64' },
        ],
        [
          { id: 'h1', description: 'Два числа', input: '10 20', expectedOutput: '(2,)\n1\nfloat64' },
          { id: 'h2', description: 'Пять чисел', input: '1 1 2 3 5', expectedOutput: '(5,)\n1\nfloat64' },
        ],
        `
          import numpy as np

          numbers = list(map(float, input().split()))
          x = np.array(numbers, dtype=float)
          print(x.shape)
          print(x.ndim)
          print(x.dtype)
        `,
      ),
    ),
  ],
)

const topicNumpyIndexing = numpyTopic(
  'numpy-indexing-slices',
  '2.4 Индексация и срезы',
  4,
  'Учимся доставать элементы, срезы, строки и столбцы NumPy-массивов.',
  'Индексация начинается с нуля, а срез `start:stop` не включает `stop`.',
  ['индексация', 'срез', 'row', 'column'],
  ['x[start:stop]', 'X[row, column]'],
  ['`x[0]` - первый элемент; `x[-1]` - последний; `X[:, 0]` - первый столбец.'],
  [
    theoryStep(
      'numpy-indexing-1d',
      'Индексация в одномерном массиве',
      'Индексация начинается с нуля.',
      [
        section('basic', 'Индексы в одномерном массиве', [
          'Индексация начинается с нуля. `x[0]` - первый элемент, `x[1]` - второй элемент.',
          'Отрицательные индексы идут с конца: `x[-1]` - последний элемент, `x[-2]` - предпоследний.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30])

              print(x[0])
              print(x[-1])
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Индексация нужна, чтобы взять один объект, один ответ target или конкретное значение признака.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-indexing-slice-1d',
      'Срезы в одномерном массиве',
      'Срез берёт диапазон элементов.',
      [
        section('slice', 'Синтаксис среза', [
          'Срез `x[start:stop]` берёт элементы от `start` до `stop - 1`. Значение `stop` не включается.',
          'Если `start` пропущен, срез начинается с начала массива. Если `stop` пропущен, срез идёт до конца.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30, 40, 50])

              print(x[1:4])
              print(x[:3])
              print(x[2:])
            `),
          ],
        }),
        section('summary', 'Короткий вывод', [
          'Срезы удобны для выбора диапазона объектов, признаков или временного окна.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-indexing-2d',
      'Индексация в двумерном массиве',
      'Для матрицы используют пару индексов.',
      [
        section('matrix', 'Индексы строки и столбца', [
          'Для двумерного массива используют запись `X[row, column]`: сначала номер строки, затем номер столбца.',
          'Если воспринимать `X` как таблицу объектов и признаков, строка - объект, столбец - признак.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2, 3],
                            [4, 5, 6]])

              print(X[0, 1])
            `, '2'),
          ],
        }),
        section('summary', 'Короткий вывод', [
          '`X[0, 1]` означает: возьми первую строку и второй столбец.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-indexing-rows-columns',
      'Строки и столбцы',
      'Двоеточие помогает выбрать всю строку или весь столбец.',
      [
        section('rows-columns', 'Выбор строк и столбцов', [
          '`X[0, :]` - первая строка. `X[:, 0]` - первый столбец.',
          'Двоеточие означает “взять все значения по этой оси”. Поэтому `:` в позиции столбца берёт все столбцы, а `:` в позиции строки берёт все строки.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2, 3],
                            [4, 5, 6]])

              print(X[0, :])
              print(X[:, 0])
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'В ML так удобно доставать один объект, один признак по всем объектам или часть матрицы признаков.',
        ]),
      ],
    ),
    quizStep(
      'numpy-indexing-quiz-index',
      'Что вернёт индекс',
      'Проверяем отрицательный индекс.',
      singleQuiz(
        'quiz-numpy-indexing-index',
        'Индекс',
        'numpy-indexing-slices',
        'numpy-ml',
        'Что вернёт `x[-1]`, если `x = np.array([10, 20, 30])`?',
        [
          { id: 'a', text: '10' },
          { id: 'b', text: '20' },
          { id: 'c', text: '30' },
          { id: 'd', text: 'Ошибку' },
        ],
        'c',
        '`x[-1]` берёт последний элемент массива.',
      ),
    ),
    quizStep(
      'numpy-indexing-quiz-slice',
      'Что вернёт срез',
      'Проверяем границы `start:stop`.',
      singleQuiz(
        'quiz-numpy-indexing-slice',
        'Срез',
        'numpy-indexing-slices',
        'numpy-ml',
        'Что вернёт `x[1:3]`, если `x = np.array([10, 20, 30, 40])`?',
        [
          { id: 'a', text: '`[10, 20]`' },
          { id: 'b', text: '`[20, 30]`' },
          { id: 'c', text: '`[20, 30, 40]`' },
          { id: 'd', text: '`[30, 40]`' },
        ],
        'b',
        'Срез начинается с индекса 1 и не включает индекс 3, поэтому остаются 20 и 30.',
      ),
    ),
    practiceStep(
      'numpy-indexing-practice',
      'Достать элементы',
      'Практика на индексы и срезы.',
      makeStdinTask(
        'task-numpy-indexing',
        'Достать элементы',
        'На вход подаются числа. Выведите первый элемент, последний элемент и срез со второго по четвёртый элемент включительно, если считать по человеческой нумерации.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте массив

          # TODO: выведите первый элемент

          # TODO: выведите последний элемент

          # TODO: выведите нужный срез
        `,
        [
          { id: 's1', description: 'Пять чисел', input: '10 20 30 40 50', expectedOutput: '10\n50\n[20 30 40]' },
        ],
        [
          { id: 'h1', description: 'Четыре числа', input: '1 2 3 4', expectedOutput: '1\n4\n[2 3 4]' },
          { id: 'h2', description: 'Шесть чисел', input: '5 6 7 8 9 10', expectedOutput: '5\n10\n[6 7 8]' },
        ],
        `
          import numpy as np

          x = np.array(list(map(int, input().split())))
          print(x[0])
          print(x[-1])
          print(x[1:4])
        `,
      ),
    ),
  ],
)

const topicNumpyVectorOps = numpyTopic(
  'numpy-vector-operations',
  '2.5 Векторные операции',
  5,
  'Изучаем операции массива с числом, операции двух массивов и идею векторизации.',
  'NumPy применяет арифметику поэлементно, если формы совместимы.',
  ['векторизация', 'поэлементная операция', 'array + scalar', 'array + array'],
  ['result = x * 2'],
  ['Векторизация делает вычисления короче и обычно быстрее, чем ручной цикл по элементам.'],
  [
    theoryStep(
      'numpy-vector-operations-scalar',
      'Массив и число',
      'Операция с числом применяется к каждому элементу массива.',
      [
        section('scalar', 'Массив и скаляр', [
          'NumPy применяет операцию к каждому элементу массива. Это относится к сложению, вычитанию, умножению, делению и многим другим операциям.',
          'Такой стиль удобен для масштабирования признаков, прибавления смещения или пересчёта единиц измерения.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])

              print(x + 10)
              print(x * 2)
            `),
          ],
        }),
        section('summary', 'Короткий вывод', [
          'Вместо цикла по элементам можно написать одну операцию над массивом.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-vector-operations-arrays',
      'Два массива',
      'Операции двух массивов обычно выполняются поэлементно.',
      [
        section('arrays', 'Поэлементные операции', [
          'Если формы совместимы, NumPy выполняет операцию поэлементно: первый элемент с первым, второй со вторым и так далее.',
          'Для одинаковых одномерных массивов результат имеет ту же форму, что и входные массивы.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              a = np.array([1, 2, 3])
              b = np.array([10, 20, 30])

              print(a + b)
              print(a * b)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Так считают ошибки, разности между предсказанием и target, произведения весов и признаков.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-vector-operations-vectorization',
      'Векторизация',
      'Векторизация - запись вычислений как операций над массивами.',
      [
        section('definition', 'Что такое векторизация', [
          'Векторизация - запись вычислений как операций над массивами, а не как ручной цикл по элементам.',
          'Для NumPy-стиля лучше выразить действие через массив целиком. Такой код обычно короче, проще читается и быстрее выполняется на больших числовых данных.',
        ], {
          codeExamples: [
            code('python', `
              result = []
              for value in x:
                  result.append(value * 2)
            `, undefined, 'Такой цикл работает, но плохо использует NumPy.'),
            code('python', `
              result = x * 2
            `, undefined, 'Векторная запись короче и обычно быстрее.'),
          ],
        }),
        section('ml', 'Применение в ML', [
          'Векторизация используется для нормализации признаков, вычисления ошибок, обновления весов и операций над батчами данных.',
        ]),
      ],
    ),
    quizStep(
      'numpy-vector-operations-quiz-result',
      'Результат операции',
      'Проверяем умножение массива на число.',
      singleQuiz(
        'quiz-numpy-vector-operations-result',
        'Операция с числом',
        'numpy-vector-operations',
        'numpy-ml',
        'Что получится при `np.array([1, 2, 3]) * 2`?',
        [
          { id: 'a', text: '`[1, 2, 3, 1, 2, 3]`' },
          { id: 'b', text: '`[2, 4, 6]`' },
          { id: 'c', text: '`[3, 4, 5]`' },
          { id: 'd', text: 'Ошибка' },
        ],
        'b',
        'NumPy умножает каждый элемент массива на 2.',
      ),
    ),
    quizStep(
      'numpy-vector-operations-quiz-loop',
      'Почему не цикл',
      'Проверяем идею векторизации.',
      singleQuiz(
        'quiz-numpy-vector-operations-loop',
        'Векторизация',
        'numpy-vector-operations',
        'numpy-ml',
        'Почему в NumPy обычно предпочитают векторные операции?',
        [
          { id: 'a', text: 'Они обычно короче и быстрее для численных массивов' },
          { id: 'b', text: 'Они всегда заменяют любые модели' },
          { id: 'c', text: 'Они нужны только для строк' },
          { id: 'd', text: 'Они запрещают использовать массивы' },
        ],
        'a',
        'Векторные операции короче и обычно используют оптимизированные NumPy-вычисления.',
      ),
    ),
    practiceStep(
      'numpy-vector-operations-practice',
      'Выполнить операции',
      'Практика на операции двух массивов.',
      makeStdinTask(
        'task-numpy-vector-operations',
        'Выполнить операции',
        'На вход подаются две строки чисел одинаковой длины. Создайте два массива. Выведите сумму массивов, произведение массивов и первый массив, умноженный на 2.',
        `
          import numpy as np

          # TODO: считайте первую строку чисел

          # TODO: считайте вторую строку чисел

          # TODO: создайте два массива

          # TODO: выполните операции

          # TODO: выведите результаты
        `,
        [
          { id: 's1', description: 'Три элемента', input: '1 2 3\n10 20 30', expectedOutput: '[11 22 33]\n[10 40 90]\n[2 4 6]' },
        ],
        [
          { id: 'h1', description: 'Два элемента', input: '5 6\n1 2', expectedOutput: '[6 8]\n[ 5 12]\n[10 12]' },
          { id: 'h2', description: 'Четыре элемента', input: '1 1 2 2\n3 4 5 6', expectedOutput: '[4 5 7 8]\n[ 3  4 10 12]\n[2 2 4 4]' },
        ],
        `
          import numpy as np

          a = np.array(list(map(int, input().split())))
          b = np.array(list(map(int, input().split())))
          print(a + b)
          print(a * b)
          print(a * 2)
        `,
      ),
    ),
  ],
)

const topicNumpyAggregations = numpyTopic(
  'numpy-aggregations-statistics',
  '2.6 Агрегации и статистики',
  6,
  'Считаем сумму, среднее, минимум, максимум, стандартное отклонение и медиану.',
  'Агрегации сворачивают массив в одно или несколько статистических значений.',
  ['np.sum', 'np.mean', 'np.min', 'np.max', 'np.std', 'np.median'],
  ['mean = sum / n'],
  ['Агрегации нужны для анализа признаков, ошибок, выбросов и baseline-решений.'],
  [
    theoryStep(
      'numpy-aggregations-sum-mean',
      '`sum`, `mean`',
      '`np.sum()` считает сумму, `np.mean()` - среднее.',
      [
        section('functions', 'Сумма и среднее', [
          '`np.sum(x)` возвращает сумму элементов массива. `np.mean(x)` возвращает среднее значение.',
          'Среднее часто используют как простой baseline для регрессии или как описание признака.',
          'Если не указан `axis`, результатом обычно становится одно число. С `axis` можно получить статистику по строкам или столбцам.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30])

              print(np.sum(x))
              print(np.mean(x))
            `),
          ],
        }),
        section('ml', 'Применение', [
          'Сумма и среднее встречаются в средней цене, средней ошибке, суммарной потере и среднем значении признака.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-aggregations-min-max',
      '`min`, `max`',
      '`np.min()` и `np.max()` находят границы значений.',
      [
        section('functions', 'Минимум и максимум', [
          '`np.min(x)` возвращает минимальное значение массива. `np.max(x)` возвращает максимальное значение.',
          'Эти функции помогают быстро понять диапазон признака и заметить странные значения.',
          'Как и другие агрегации, они поддерживают параметр `axis`, если нужно искать минимум или максимум по строкам либо столбцам.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30])

              print(np.min(x))
              print(np.max(x))
            `),
          ],
        }),
        section('ml', 'Применение', [
          'Минимум и максимум используют для проверки диапазона признака, анализа выбросов и масштабирования данных.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-aggregations-std-median',
      '`std`, `median`',
      '`np.std()` показывает разброс, `np.median()` - центральное значение.',
      [
        section('functions', 'Стандартное отклонение и медиана', [
          '`np.std(x)` считает стандартное отклонение, то есть типичный разброс значений вокруг среднего. `np.median(x)` возвращает медиану.',
          'Медиана устойчивее к выбросам, чем среднее. Если в массиве есть очень большое значение, среднее может сильно сдвинуться, а медиана останется ближе к основной массе данных.',
          'Обе функции возвращают статистическое значение для массива или набор значений по выбранной оси.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30, 1000])

              print(np.mean(x))
              print(np.median(x))
              print(np.std(x))
            `),
          ],
        }),
        section('ml', 'Применение', [
          '`std` помогает оценивать масштаб признаков, а `median` часто используют при анализе выбросов и заполнении пропусков.',
        ]),
      ],
    ),
    quizStep(
      'numpy-aggregations-quiz-mean',
      'Выбрать статистику',
      'Проверяем функцию среднего.',
      singleQuiz(
        'quiz-numpy-aggregations-mean',
        'Среднее значение',
        'numpy-aggregations-statistics',
        'numpy-ml',
        'Какую функцию использовать для среднего значения массива?',
        [
          { id: 'a', text: '`np.max`' },
          { id: 'b', text: '`np.mean`' },
          { id: 'c', text: '`np.arange`' },
          { id: 'd', text: '`np.shape`' },
        ],
        'b',
        '`np.mean()` считает среднее значение массива.',
      ),
    ),
    quizStep(
      'numpy-aggregations-quiz-max',
      'Что вернёт агрегация',
      'Проверяем максимум.',
      singleQuiz(
        'quiz-numpy-aggregations-max',
        'Максимум',
        'numpy-aggregations-statistics',
        'numpy-ml',
        'Что вернёт `np.max(np.array([3, 10, 5]))`?',
        [
          { id: 'a', text: '3' },
          { id: 'b', text: '5' },
          { id: 'c', text: '10' },
          { id: 'd', text: '18' },
        ],
        'c',
        '`np.max()` возвращает наибольшее значение массива.',
      ),
    ),
    practiceStep(
      'numpy-aggregations-practice',
      'Посчитать статистики',
      'Практика на базовые агрегации.',
      makeStdinTask(
        'task-numpy-statistics',
        'Посчитать статистики',
        'На вход подаются числа. Выведите сумму, среднее, минимум и максимум.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте массив

          # TODO: посчитайте сумму, среднее, минимум и максимум

          # TODO: выведите результаты
        `,
        [
          { id: 's1', description: 'Три числа', input: '10 20 30', expectedOutput: '60\n20.0\n10\n30' },
        ],
        [
          { id: 'h1', description: 'Разные значения', input: '3 10 5', expectedOutput: '18\n6.0\n3\n10' },
          { id: 'h2', description: 'Одинаковые значения', input: '7 7 7 7', expectedOutput: '28\n7.0\n7\n7' },
        ],
        `
          import numpy as np

          x = np.array(list(map(int, input().split())))
          print(np.sum(x))
          print(np.mean(x))
          print(np.min(x))
          print(np.max(x))
        `,
      ),
    ),
  ],
)

const topicNumpyAxis = numpyTopic(
  'numpy-2d-axis',
  '2.7 Двумерные массивы и axis',
  7,
  'Разбираем матрицу объектов-признаков, `axis=0`, `axis=1` и `reshape()`.',
  '`axis=0` агрегирует по строкам и даёт результат по столбцам; `axis=1` агрегирует по столбцам и даёт результат по строкам.',
  ['axis', 'reshape', 'матрица', 'строка', 'столбец'],
  ['X.mean(axis=0)', 'X.mean(axis=1)', 'x.reshape(new_shape)'],
  ['Для матрицы объектов и признаков `axis=0` часто даёт статистику по каждому признаку.'],
  [
    theoryStep(
      'numpy-2d-axis-matrix',
      'Матрица объектов и признаков',
      'В ML таблицу часто представляют как двумерный массив.',
      [
        section('matrix', 'Матрица X', [
          'В ML таблицу часто представляют как двумерный массив: строки - объекты, столбцы - признаки.',
          'Например, одна строка описывает квартиру, а столбцы хранят площадь и количество комнат.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[30, 1],
                            [45, 2],
                            [60, 3]])

              print(X.shape)
            `),
          ],
        }),
        section('summary', 'Короткий вывод', [
          'Форма `(3, 2)` означает 3 объекта и 2 признака.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-2d-axis-0',
      '`axis=0`',
      '`axis=0` обычно даёт результат по столбцам.',
      [
        section('axis0', 'Как читать axis=0', [
          '`axis=0` означает: выполнить операцию вдоль строк, получить результат по столбцам.',
          'Для матрицы объектов и признаков это часто значит “посчитать статистику для каждого признака”.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2, 3],
                            [4, 5, 6]])

              print(X.mean(axis=0))
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Так можно получить среднее значение каждого признака перед центрированием или масштабированием.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-2d-axis-1',
      '`axis=1`',
      '`axis=1` обычно даёт результат по строкам.',
      [
        section('axis1', 'Как читать axis=1', [
          '`axis=1` означает: выполнить операцию вдоль столбцов, получить результат по строкам.',
          'Для матрицы объектов и признаков это может означать одну статистику для каждого объекта.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2, 3],
                            [4, 5, 6]])

              print(X.mean(axis=1))
            `),
          ],
        }),
        section('memory', 'Как запомнить', [
          'Если результат должен иметь длину по числу столбцов, чаще всего нужен `axis=0`. Если результат должен иметь длину по числу строк, чаще всего нужен `axis=1`.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-2d-axis-reshape',
      '`reshape`',
      '`reshape()` меняет форму массива без изменения значений.',
      [
        section('syntax', 'Синтаксис reshape', [
          '`x.reshape(new_shape)` меняет форму массива без изменения значений.',
          'Важно: общее количество элементов должно совпадать. Массив из 6 элементов можно превратить в `(2, 3)` или `(3, 2)`, но нельзя в `(4, 2)`.',
          'Параметр `new_shape` задаёт новую форму. Результат - массив с теми же значениями, но с другой структурой осей.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.arange(6)
              X = x.reshape(2, 3)

              print(X)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          '`reshape()` часто нужен при подготовке данных к модели, превращении вектора в матрицу и работе с батчами.',
        ]),
      ],
    ),
    quizStep(
      'numpy-2d-axis-quiz-axis',
      'Что означает axis',
      'Проверяем `axis=0`.',
      singleQuiz(
        'quiz-numpy-2d-axis-axis',
        'Axis',
        'numpy-2d-axis',
        'numpy-ml',
        'Что обычно даёт `X.mean(axis=0)` для матрицы объектов-признаков?',
        [
          { id: 'a', text: 'Среднее по каждому столбцу-признаку' },
          { id: 'b', text: 'Среднее по каждой строке-объекту' },
          { id: 'c', text: 'Только первый элемент' },
          { id: 'd', text: 'Ошибку всегда' },
        ],
        'a',
        '`axis=0` сворачивает строки и оставляет результат по столбцам, то есть по признакам.',
      ),
    ),
    quizStep(
      'numpy-2d-axis-quiz-reshape',
      'Форма после reshape',
      'Проверяем форму результата.',
      singleQuiz(
        'quiz-numpy-2d-axis-reshape',
        'Reshape',
        'numpy-2d-axis',
        'numpy-ml',
        'Какая форма будет у `np.arange(6).reshape(2, 3)`?',
        [
          { id: 'a', text: '`(6,)`' },
          { id: 'b', text: '`(3, 2)`' },
          { id: 'c', text: '`(2, 3)`' },
          { id: 'd', text: '`(1, 6)`' },
        ],
        'c',
        '`reshape(2, 3)` явно задаёт форму 2 строки и 3 столбца.',
      ),
    ),
    practiceStep(
      'numpy-2d-axis-practice',
      'Средние по строкам и столбцам',
      'Практика на `reshape` и `axis`.',
      makeStdinTask(
        'task-numpy-axis-means',
        'Средние по строкам и столбцам',
        'На вход подаются 6 чисел. Создайте матрицу формы `(2, 3)`. Выведите среднее по столбцам и среднее по строкам.',
        `
          import numpy as np

          # TODO: считайте 6 чисел

          # TODO: создайте массив и измените форму на (2, 3)

          # TODO: посчитайте среднее по столбцам

          # TODO: посчитайте среднее по строкам

          # TODO: выведите результаты
        `,
        [
          { id: 's1', description: 'Последовательность 1..6', input: '1 2 3 4 5 6', expectedOutput: '[2.5 3.5 4.5]\n[2. 5.]' },
        ],
        [
          { id: 'h1', description: 'Десятки', input: '10 20 30 40 50 60', expectedOutput: '[25. 35. 45.]\n[20. 50.]' },
          { id: 'h2', description: 'Одинаковые', input: '1 1 1 1 1 1', expectedOutput: '[1. 1. 1.]\n[1. 1.]' },
        ],
        `
          import numpy as np

          values = list(map(float, input().split()))
          X = np.array(values).reshape(2, 3)
          print(X.mean(axis=0))
          print(X.mean(axis=1))
        `,
      ),
    ),
  ],
)

const topicNumpyMasks = numpyTopic(
  'numpy-masks-where',
  '2.8 Маски и np.where',
  8,
  'Используем булевы маски, фильтрацию и `np.where()`.',
  'Булева маска выбирает элементы по условию, а `np.where()` заменяет значения по условию.',
  ['булева маска', 'фильтрация', 'np.where'],
  ['mask = x > 10', 'np.where(condition, value_if_true, value_if_false)'],
  ['Маски нужны для фильтрации значений, поиска выбросов и условной замены.'],
  [
    theoryStep(
      'numpy-masks-mask',
      'Булевы маски',
      'Булева маска - массив из True и False.',
      [
        section('mask', 'Что такое булева маска', [
          'Булева маска - массив из `True` и `False`, который получается после условия.',
          'Каждый элемент маски отвечает на вопрос, выполняется ли условие для соответствующего элемента исходного массива.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([5, 12, 7, 20])

              mask = x > 10
              print(mask)
            `, '[False  True False  True]'),
          ],
        }),
        section('ml', 'Где используется', [
          'Маски помогают выбирать объекты по условию, проверять выбросы и строить признаки на основе логических правил.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-masks-filter',
      'Фильтрация массива',
      'Маску можно применить к массиву.',
      [
        section('filter', 'Фильтрация по маске', [
          'Маску можно использовать, чтобы выбрать только подходящие элементы. Запись `x[mask]` вернёт элементы, для которых в маске стоит `True`.',
          'Форма результата зависит от того, сколько элементов прошло условие.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([5, 12, 7, 20])

              mask = x > 10
              print(x[mask])
            `, '[12 20]'),
          ],
        }),
        section('use-cases', 'Применение', [
          'Так можно выбрать большие значения, убрать выбросы или оставить объекты по условию.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-masks-where',
      '`np.where()`',
      '`np.where()` выбирает значения по условию.',
      [
        section('syntax', 'Синтаксис where', [
          '`np.where(condition, value_if_true, value_if_false)` выбирает значения по условию.',
          'Если условие истинно, берётся `value_if_true`, иначе - `value_if_false`. Результат обычно имеет форму условия.',
          'Параметр `condition` обычно является булевой маской. Возвращаемый `ndarray` содержит значения, собранные по этому условию.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([5, 12, 7, 20])

              labels = np.where(x > 10, "big", "small")
              print(labels)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          '`np.where()` удобно применять для простого rule-based признака, бинарной метки, ограничения значений или замены выбросов.',
        ]),
      ],
    ),
    quizStep(
      'numpy-masks-quiz-mask',
      'Что вернёт маска',
      'Проверяем результат условия.',
      singleQuiz(
        'quiz-numpy-masks-mask',
        'Булева маска',
        'numpy-masks-where',
        'numpy-ml',
        'Что вернёт `np.array([1, 5, 10]) > 4`?',
        [
          { id: 'a', text: '`[False, True, True]`' },
          { id: 'b', text: '`[True, False, False]`' },
          { id: 'c', text: '`[1, 5, 10]`' },
          { id: 'd', text: '`[5, 10]`' },
        ],
        'a',
        'Условие `> 4` не выполняется для 1 и выполняется для 5 и 10.',
      ),
    ),
    quizStep(
      'numpy-masks-quiz-where',
      'Что выберет where',
      'Проверяем условную замену.',
      singleQuiz(
        'quiz-numpy-masks-where',
        'np.where',
        'numpy-masks-where',
        'numpy-ml',
        'Что делает `np.where(x > 0, 1, 0)`?',
        [
          { id: 'a', text: 'Заменяет положительные значения на 1, остальные на 0' },
          { id: 'b', text: 'Удаляет все нули' },
          { id: 'c', text: 'Сортирует массив' },
          { id: 'd', text: 'Меняет форму массива' },
        ],
        'a',
        '`np.where()` выбирает 1 там, где условие истинно, и 0 там, где оно ложно.',
      ),
    ),
    practiceStep(
      'numpy-masks-practice',
      'Отфильтровать значения',
      'Практика на булевую маску.',
      makeStdinTask(
        'task-numpy-filter-values',
        'Отфильтровать значения',
        'На вход подаются числа. Выведите только числа больше 10.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте массив

          # TODO: создайте маску

          # TODO: примените маску

          # TODO: выведите результат
        `,
        [
          { id: 's1', description: 'Есть значения больше 10', input: '5 12 7 20', expectedOutput: '[12 20]' },
        ],
        [
          { id: 'h1', description: 'Нет значений больше 10', input: '1 2 3', expectedOutput: '[]' },
          { id: 'h2', description: 'Граница и значения выше', input: '11 10 15', expectedOutput: '[11 15]' },
        ],
        `
          import numpy as np

          x = np.array(list(map(int, input().split())))
          mask = x > 10
          filtered = x[mask]
          print(filtered)
        `,
      ),
    ),
  ],
)

const topicNumpyBroadcasting = numpyTopic(
  'numpy-broadcasting',
  '2.9 Broadcasting',
  9,
  'Разбираем broadcasting на числе, векторе и центрировании признаков.',
  'Broadcasting позволяет выполнять операции с массивами разных, но совместимых форм.',
  ['broadcasting', 'scalar', 'vector', 'centered features'],
  ['X_centered = X - X.mean(axis=0)'],
  ['Broadcasting особенно полезен при нормализации и центрировании признаков.'],
  [
    theoryStep(
      'numpy-broadcasting-definition',
      'Что такое broadcasting',
      'Broadcasting расширяет меньший массив до совместимой формы логически, без лишних копий.',
      [
        section('definition', 'Идея broadcasting', [
          'Broadcasting - правило NumPy, которое позволяет выполнять операции с массивами разных, но совместимых форм.',
          'Идея такая: меньший массив как будто “растягивается” до формы большего, но физически лишние копии не создаются.',
        ]),
        section('why', 'Зачем это нужно', [
          'Без broadcasting пришлось бы вручную создавать копии значений нужной формы. С broadcasting код для нормализации и сдвига признаков становится короче и понятнее.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-scalar',
      'Массив + число',
      'Число применяется к каждому элементу массива.',
      [
        section('scalar', 'Скаляр и массив', [
          'Самый простой пример broadcasting - операция массива с числом. Число применяется к каждому элементу массива.',
          'Физически NumPy не создаёт массив `[10, 10, 10]`, но результат получается таким, как будто число было повторено.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])
              print(x + 10)
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Так можно прибавить смещение, умножить признак на коэффициент или перевести единицы измерения.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-vector',
      'Матрица + вектор',
      'Вектор может примениться к каждой строке матрицы.',
      [
        section('matrix-vector', 'Матрица и вектор', [
          'Если форма матрицы `(3, 2)`, а форма вектора `(2,)`, NumPy может добавить этот вектор к каждой строке матрицы.',
          'Это работает, потому что последняя размерность совпадает: в каждой строке матрицы два значения и в векторе два значения.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2],
                            [3, 4],
                            [5, 6]])

              v = np.array([10, 20])

              print(X + v)
            `),
          ],
        }),
        section('summary', 'Короткий вывод', [
          'Вектор добавляется к каждой строке матрицы.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-centering',
      'Центрирование признаков',
      'Broadcasting часто используют для нормализации признаков.',
      [
        section('centering', 'Центрирование признаков', [
          'В ML broadcasting часто используют для нормализации и центрирования признаков.',
          '`mean = X.mean(axis=0)` содержит среднее по каждому признаку. Когда мы пишем `X - mean`, NumPy вычитает этот вектор из каждой строки матрицы.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2],
                            [3, 4],
                            [5, 6]])

              mean = X.mean(axis=0)
              X_centered = X - mean

              print(X_centered)
            `),
          ],
        }),
        section('ml', 'Зачем центрировать', [
          'Центрирование делает среднее значение признака равным нулю. Это полезно для многих алгоритмов и для анализа масштаба признаков.',
        ]),
      ],
    ),
    quizStep(
      'numpy-broadcasting-quiz-compatible',
      'Можно ли выполнить операцию',
      'Проверяем совместимость `(3, 2)` и `(2,)`.',
      singleQuiz(
        'quiz-numpy-broadcasting-compatible',
        'Совместимость форм',
        'numpy-broadcasting',
        'numpy-ml',
        'Можно ли сложить массив формы `(3, 2)` и вектор формы `(2,)`?',
        [
          { id: 'a', text: 'Да, вектор применится к каждой строке' },
          { id: 'b', text: 'Нет, формы всегда должны быть полностью одинаковыми' },
          { id: 'c', text: 'Да, но только если все значения нулевые' },
          { id: 'd', text: 'Нет, NumPy не поддерживает сложение массивов' },
        ],
        'a',
        'Вектор формы `(2,)` совместим с последней размерностью матрицы `(3, 2)`, поэтому применится к каждой строке.',
      ),
    ),
    quizStep(
      'numpy-broadcasting-quiz-shape',
      'Форма результата',
      'Проверяем форму после broadcasting.',
      singleQuiz(
        'quiz-numpy-broadcasting-shape',
        'Форма результата',
        'numpy-broadcasting',
        'numpy-ml',
        'Какая форма будет у результата `X + v`, если `X.shape == (3, 2)`, а `v.shape == (2,)`?',
        [
          { id: 'a', text: '`(2,)`' },
          { id: 'b', text: '`(3,)`' },
          { id: 'c', text: '`(3, 2)`' },
          { id: 'd', text: '`(2, 3)`' },
        ],
        'c',
        'Результат операции с матрицей сохраняет форму матрицы `(3, 2)`.',
      ),
    ),
    practiceStep(
      'numpy-broadcasting-practice',
      'Центрировать матрицу',
      'Практика на центрирование признаков.',
      makeStdinTask(
        'task-numpy-center-matrix',
        'Центрировать матрицу',
        'На вход подаются 6 чисел. Создайте матрицу формы `(3, 2)`. Вычтите из каждого столбца его среднее. Выведите результат.',
        `
          import numpy as np

          # TODO: считайте 6 чисел

          # TODO: создайте матрицу формы (3, 2)

          # TODO: найдите среднее по столбцам

          # TODO: вычтите среднее из матрицы

          # TODO: выведите результат
        `,
        [
          { id: 's1', description: 'Последовательность 1..6', input: '1 2 3 4 5 6', expectedOutput: '[[-2. -2.]\n [ 0.  0.]\n [ 2.  2.]]' },
        ],
        [
          { id: 'h1', description: 'Сдвинутая последовательность', input: '2 4 4 6 6 8', expectedOutput: '[[-2. -2.]\n [ 0.  0.]\n [ 2.  2.]]' },
          { id: 'h2', description: 'Одинаковые строки', input: '1 1 1 1 1 1', expectedOutput: '[[0. 0.]\n [0. 0.]\n [0. 0.]]' },
        ],
        `
          import numpy as np

          values = list(map(float, input().split()))
          X = np.array(values).reshape(3, 2)
          mean = X.mean(axis=0)
          centered = X - mean
          print(centered)
        `,
      ),
    ),
  ],
)

const topicNumpyRandom = numpyTopic(
  'numpy-random-reproducibility',
  '2.10 Random и воспроизводимость',
  10,
  'Используем `default_rng`, seed, случайные целые числа, нормальное распределение и перестановки.',
  'Seed фиксирует генератор, чтобы случайный эксперимент можно было повторить.',
  ['random', 'seed', 'default_rng', 'integers', 'normal', 'permutation'],
  ['rng = np.random.default_rng(seed)'],
  ['Случайность нужна для перемешивания, train/test split, инициализации весов и синтетических данных.'],
  [
    theoryStep(
      'numpy-random-why',
      'Зачем нужна случайность',
      'Случайность в ML нужна часто, но она должна быть управляемой.',
      [
        section('uses', 'Где нужна случайность', [
          'Случайность в ML нужна часто: перемешать данные, разделить train/test, инициализировать веса, создать синтетические данные или добавить шум.',
          'Но случайность должна быть управляемой. Иначе один и тот же эксперимент может давать разные результаты, и будет трудно понять, что именно изменилось.',
        ], {
          bullets: ['перемешать данные', 'разделить train/test', 'инициализировать веса', 'создать синтетические данные', 'добавить шум'],
        }),
        section('summary', 'Короткий вывод', [
          'Для учебных примеров, тестов и сравнения моделей случайность обычно фиксируют через seed.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-random-seed',
      'Seed и воспроизводимость',
      'Seed позволяет повторить последовательность псевдослучайных чисел.',
      [
        section('seed', 'Что делает seed', [
          'Seed фиксирует начальное состояние генератора случайных чисел. Если seed одинаковый, результат можно повторить.',
          'Это не делает числа “неслучайными” по смыслу эксперимента, но делает последовательность воспроизводимой для отладки и сравнения.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              rng = np.random.default_rng(42)

              print(rng.integers(0, 10, size=5))
            `),
          ],
        }),
        section('ml', 'Где используется', [
          'Seed указывают при разделении данных, генерации синтетических примеров и запуске экспериментов, которые нужно сравнить честно.',
        ]),
      ],
    ),
    theoryStep(
      'numpy-random-functions',
      '`default_rng`, `integers`, `normal`, `permutation`',
      'Основные методы современного генератора NumPy.',
      [
        section('functions', 'Функции генератора', [
          '`np.random.default_rng(seed)` создаёт генератор. `rng.integers(low, high, size)` возвращает случайные целые числа.',
          '`rng.normal(loc, scale, size)` генерирует значения из нормального распределения. `rng.permutation(x)` возвращает случайную перестановку элементов.',
          '`low` и `high` задают диапазон для целых чисел, `loc` и `scale` - среднее и стандартное отклонение нормального распределения, `size` - форму результата.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              rng = np.random.default_rng(42)

              print(rng.integers(0, 10, size=3))
              print(rng.normal(0, 1, size=3))
              print(rng.permutation([1, 2, 3, 4]))
            `),
          ],
        }),
        section('ml', 'Где используется', [
          '`integers` полезен для учебных данных и случайных индексов. `normal` часто используют для шума и синтетических признаков. `permutation` нужна для перемешивания объектов.',
        ]),
      ],
    ),
    quizStep(
      'numpy-random-quiz-seed',
      'Зачем нужен seed',
      'Проверяем воспроизводимость.',
      singleQuiz(
        'quiz-numpy-random-seed',
        'Seed',
        'numpy-random-reproducibility',
        'numpy-ml',
        'Зачем фиксировать seed?',
        [
          { id: 'a', text: 'Чтобы эксперимент можно было повторить' },
          { id: 'b', text: 'Чтобы массивы всегда были только нулевыми' },
          { id: 'c', text: 'Чтобы NumPy работал без Python' },
          { id: 'd', text: 'Чтобы отключить все случайные числа' },
        ],
        'a',
        'Одинаковый seed позволяет повторить последовательность псевдослучайных чисел.',
      ),
    ),
    quizStep(
      'numpy-random-quiz-function',
      'Какую функцию выбрать',
      'Проверяем перестановку.',
      singleQuiz(
        'quiz-numpy-random-function',
        'Функции random',
        'numpy-random-reproducibility',
        'numpy-ml',
        'Какая функция создаёт случайную перестановку элементов?',
        [
          { id: 'a', text: '`rng.normal`' },
          { id: 'b', text: '`rng.integers`' },
          { id: 'c', text: '`rng.permutation`' },
          { id: 'd', text: '`np.array`' },
        ],
        'c',
        '`rng.permutation()` возвращает случайную перестановку элементов.',
      ),
    ),
    practiceStep(
      'numpy-random-practice',
      'Сгенерировать данные',
      'Практика на `default_rng()` и `integers()`.',
      makeStdinTask(
        'task-numpy-random-integers',
        'Сгенерировать данные',
        'На вход подаются `seed` и `n`. Создайте генератор `np.random.default_rng(seed)`. Сгенерируйте `n` случайных целых чисел от 0 до 10 не включая 10. Выведите массив.',
        `
          import numpy as np

          # TODO: считайте seed и n

          # TODO: создайте генератор

          # TODO: сгенерируйте случайные целые числа

          # TODO: выведите массив
        `,
        [
          { id: 's1', description: 'Seed 42', input: '42 5', expectedOutput: '[0 7 6 4 4]' },
        ],
        [
          { id: 'h1', description: 'Seed 1', input: '1 4', expectedOutput: '[4 5 7 9]' },
          { id: 'h2', description: 'Seed 7', input: '7 3', expectedOutput: '[9 6 6]' },
        ],
        `
          import numpy as np

          seed, n = map(int, input().split())
          rng = np.random.default_rng(seed)
          values = rng.integers(0, 10, size=n)
          print(values)
        `,
      ),
    ),
  ],
)

export const numpyTopics: FlowTopic[] = [
  topicNumpyWhy,
  topicNumpyCreation,
  topicNumpyShape,
  topicNumpyIndexing,
  topicNumpyVectorOps,
  topicNumpyAggregations,
  topicNumpyAxis,
  topicNumpyMasks,
  topicNumpyBroadcasting,
  topicNumpyRandom,
]
