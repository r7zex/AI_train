import { callout, code, makeStdinTask, pandasTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicPandasWhy = pandasTopic(
  'pandas-why-dataframe',
  '3.1 Зачем нужен pandas и что такое DataFrame',
  1,
  'Разбираем роль pandas в ML, объекты Series и DataFrame, отличие pandas от NumPy.',
  'Pandas помогает работать с табличными данными до обучения модели: загрузить таблицу, посмотреть признаки, найти пропуски и подготовить X и y.',
  ['pandas', 'DataFrame', 'Series', 'columns', 'index'],
  ['DataFrame = таблица строк и именованных столбцов', 'Series = один столбец DataFrame'],
  [
    'DataFrame хранит таблицу с названиями столбцов.',
    'Series обычно появляется при выборе одного столбца.',
    'NumPy удобен для числовых массивов, pandas - для таблиц с разными типами данных.',
  ],
  [
    theoryStep(
      'pandas-why-scenario',
      'Сценарий табличного ML',
      'Показываем путь от CSV до модели.',
      [
        section('flow', 'CSV -> DataFrame -> X и y', [
          'Типичный pandas-сценарий начинается с CSV или другой таблицы. Мы читаем данные в `DataFrame`, осматриваем строки и типы, чистим пропуски, создаём признаки, а затем выделяем `X` и `y` для модели.',
          'Так pandas закрывает этап, где данные ещё человекочитаемые: у столбцов есть имена, у строк есть индекс, а разные признаки могут иметь разные типы. NumPy и sklearn подключаются позже, когда таблица уже приведена к числовой форме.',
        ], {
          callouts: [
            callout('Рабочая цепочка', 'CSV -> DataFrame -> осмотр -> очистка -> признаки `X` и target `y` -> модель.', 'schema'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-why-ml',
      'Зачем pandas нужен в ML',
      'Pandas закрывает этап работы с таблицами до модели.',
      [
        section('role', 'Таблица до обучения модели', [
          'Pandas нужен, когда данные лежат в таблице: квартиры, клиенты, заказы, результаты экспериментов или строки CSV-файла. В ML такая таблица почти всегда появляется до обучения модели.',
          '`DataFrame` хранит данные вместе с именами столбцов, индексом строк и типами. Поэтому можно быстро посмотреть структуру таблицы, выбрать признаки, найти пропуски, сгруппировать строки и подготовить данные для sklearn.',
          'Типичная цепочка выглядит так: `CSV -> pandas -> очистка -> X, y -> sklearn`. Модель редко обучают на сыром файле без проверки качества данных.',
        ], {
          callouts: [
            callout('Вывод', 'Pandas - это рабочий инструмент для табличного этапа ML-проекта: загрузки, просмотра, очистки и подготовки признаков.', 'summary'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-series-dataframe',
      'Series и DataFrame',
      'DataFrame - таблица, Series - один столбец.',
      [
        section('objects', 'Два главных объекта', [
          '**DataFrame** - двумерная таблица: строки идут по индексу, столбцы имеют имена. Это похоже на лист Excel или SQL-таблицу, но работает прямо в Python.',
          '**Series** - одномерный объект pandas. Чаще всего это один столбец из `DataFrame`: например `df["price"]`. У Series тоже есть индекс, поэтому значения можно сопоставлять со строками таблицы.',
          'Одинарные скобки при выборе столбца обычно возвращают `Series`, а двойные скобки - `DataFrame`. Это влияет на форму данных и на то, как дальше работает код.',
        ], {
          codeExamples: [
            code('python', `
              import pandas as pd

              df = pd.DataFrame({
                  "area": [35, 52, 80],
                  "rooms": [1, 2, 3],
                  "price": [8200000, 9100000, 18400000],
              })

              print(df)
            `, `
                 area  rooms     price
              0    35      1   8200000
              1    52      2   9100000
              2    80      3  18400000
            `, '`pd.DataFrame()` создал таблицу с тремя именованными столбцами и автоматическим индексом строк.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-vs-numpy',
      'Pandas vs NumPy',
      'NumPy работает с массивами, pandas - с таблицами.',
      [
        section('compare', 'Когда что выбирать', [
          '**NumPy** лучше подходит для компактных числовых массивов и быстрых векторных вычислений. В одном массиве обычно один общий `dtype`, а названия признаков приходится хранить отдельно.',
          '**Pandas** удобнее, когда важны названия колонок, разные типы данных, пропуски, индексы и группировки. В реальном EDA это почти всегда важнее, чем просто хранить матрицу чисел.',
          'На раннем этапе данные удобно держать в pandas, а перед обучением можно передать модели числовую таблицу признаков `X` и целевой столбец `y`.',
        ], {
          table: {
            headers: ['NumPy', 'Pandas'],
            rows: [
              ['Числовые массивы', 'Таблицы с именами столбцов'],
              ['Быстрые вычисления', 'Анализ и очистка данных'],
              ['Обычно один dtype', 'Разные типы по столбцам'],
              ['Нет названий колонок внутри массива', 'Есть `columns` и `index`'],
            ],
          },
        }),
      ],
    ),
    theoryStep(
      'pandas-why-checkpoint',
      'Что теперь умеем',
      'Фиксируем роль pandas перед практическими методами.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Теперь pandas не выглядит как “ещё одна библиотека для таблиц”: это основной рабочий слой между сырым файлом и числовой матрицей для модели.',
        ], {
          bullets: [
            'объяснять, зачем нужен `DataFrame`;',
            'отличать `DataFrame` от `Series`;',
            'понимать, почему pandas удобнее NumPy для сырой таблицы;',
            'видеть путь от CSV к `X` и `y`.',
          ],
        }),
      ],
    ),
    quizStep(
      'pandas-why-quiz-dataframe',
      'Что такое DataFrame',
      'Проверяем базовое определение DataFrame.',
      singleQuiz(
        'quiz-pandas-dataframe-definition',
        'DataFrame',
        'pandas-why-dataframe',
        'pandas-eda',
        'Какое описание лучше всего подходит для `DataFrame`?',
        [
          { id: 'a', text: 'Один числовой массив без названий столбцов' },
          { id: 'b', text: 'Таблица с именованными столбцами и индексом строк' },
          { id: 'c', text: 'Функция для обучения модели' },
          { id: 'd', text: 'Только один столбец pandas' },
        ],
        'b',
        '`DataFrame` - табличная структура pandas: строки имеют индекс, а столбцы имеют имена.',
      ),
    ),
    quizStep(
      'pandas-why-quiz-series',
      'Series или DataFrame',
      'Различаем один столбец и таблицу.',
      singleQuiz(
        'quiz-pandas-series-vs-dataframe',
        'Series и DataFrame',
        'pandas-why-dataframe',
        'pandas-eda',
        'Что обычно вернёт выражение `df["price"]`?',
        [
          { id: 'a', text: '`Series` с одним столбцом' },
          { id: 'b', text: '`DataFrame` со всеми столбцами' },
          { id: 'c', text: 'Список названий колонок' },
          { id: 'd', text: 'Файл CSV' },
        ],
        'a',
        'Выбор одного столбца через одинарные скобки обычно возвращает `Series`; для `DataFrame` используют двойные скобки: `df[["price"]]`.',
      ),
    ),
    practiceStep(
      'pandas-why-practice',
      'Создать DataFrame',
      'Создаём таблицу из словаря и выводим названия столбцов.',
      makeStdinTask(
        'task-pandas-create-dataframe',
        'Создать DataFrame',
        'Создайте `DataFrame` с колонками `name`, `age`, `city` и выведите список названий столбцов.',
        `
          import pandas as pd

          # TODO: создайте DataFrame с колонками name, age, city

          # TODO: выведите названия столбцов
        `,
        [
          { id: 's1', description: 'Названия трёх колонок', expectedOutput: "['name', 'age', 'city']" },
        ],
        [
          { id: 'h1', description: 'Проверка порядка колонок', expectedOutput: "['name', 'age', 'city']" },
        ],
        `
          import pandas as pd

          df = pd.DataFrame({
              "name": ["Anna", "Boris", "Mira"],
              "age": [24, 35, 29],
              "city": ["Moscow", "Kazan", "Perm"],
          })

          print(list(df.columns))
        `,
      ),
    ),
  ],
)
