import { callout, code, makeStdinTask, pandasTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicPandasReadInspect = pandasTopic(
  'pandas-read-inspect',
  '3.2 Загрузка данных и первый просмотр таблицы',
  2,
  'Строим сценарий первого осмотра датасета: чтение, строки, структура, сводка и пропуски.',
  'Новый датасет нельзя сразу отдавать модели: сначала нужно понять размер, колонки, типы, пропуски и странные значения.',
  ['read_csv', 'head', 'tail', 'sample', 'shape', 'columns', 'dtypes', 'info', 'describe', 'isna'],
  ['первый чек-лист: head, shape, info, describe, isna().sum()'],
  [
    '`pd.read_csv()` читает CSV в DataFrame.',
    '`head`, `tail`, `sample` показывают реальные строки.',
    '`shape`, `columns`, `dtypes`, `info`, `describe` помогают осмотреть структуру.',
  ],
  [
    theoryStep(
      'pandas-loading-first-look',
      'Первый осмотр датасета',
      'Показываем порядок действий после чтения таблицы.',
      [
        section('why', 'Сначала смотрим, что пришло', [
          'Когда открываем новый датасет, нельзя сразу обучать модель. Сначала нужно понять: сколько строк и столбцов, какие названия колонок, какие типы данных, есть ли пропуски и какие значения выглядят странно.',
          'В реальной работе это экономит часы: ошибка разделителя, лишняя колонка или строковые числа видны уже на первом осмотре.',
        ], {
          callouts: [
            callout('Первый чек-лист датасета', '`head()`\n`shape`\n`info()`\n`describe()`\n`isna().sum()`', 'summary'),
          ],
        }),
        section('groups', 'Методы по смыслу', [
          'Методы первого осмотра удобно держать группами: загрузить таблицу, посмотреть строки, посмотреть структуру, посмотреть сводку.',
        ], {
          table: {
            headers: ['Задача', 'Методы', 'Что узнаём'],
            rows: [
              ['Загрузить', '`pd.read_csv()`', 'получаем DataFrame из CSV'],
              ['Посмотреть строки', '`head()`, `tail()`, `sample()`', 'видим реальные записи'],
              ['Посмотреть структуру', '`shape`, `columns`, `dtypes`', 'размер, имена, типы'],
              ['Посмотреть сводку', '`info()`, `describe()`', 'непустые значения и статистики'],
            ],
          },
        }),
        section('example', 'Мини-осмотр таблицы квартир', [
          'Даже маленькая таблица показывает логику: сначала строки, затем размер и типы, затем пропуски.',
        ], {
          codeExamples: [
            code('python', `
              import pandas as pd

              df = pd.DataFrame({
                  "area": [35, 52, 80],
                  "rooms": [1, 2, 3],
                  "price": [8.2, 9.1, None],
              })

              print(df.head())
              print(df.shape)
              print(df.isna().sum())
            `, `
                 area  rooms  price
              0    35      1    8.2
              1    52      2    9.1
              2    80      3    NaN
              (3, 3)
              area     0
              rooms    0
              price    1
              dtype: int64
            `, 'Сразу видно размер таблицы и один пропуск в `price`.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-loading-checkpoint',
      'Что теперь умеем',
      'Закрепляем первый чек-лист.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Первый осмотр не чистит данные, но показывает, куда смотреть дальше.',
        ], {
          bullets: [
            'читать CSV через `pd.read_csv()`;',
            'проверять первые, последние и случайные строки;',
            'смотреть размер и названия колонок;',
            'проверять типы и пропуски;',
            'получать числовую сводку через `describe()`.',
          ],
          callouts: [
            callout('Типичная ошибка', 'Ограничиться `head()` и не посмотреть `tail()` или `sample()`. Первые строки могут быть нормальными, а проблемы часто прячутся дальше.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-loading-strange-values',
      'Странные значения',
      'Добавляем смысл первичного просмотра.',
      [
        section('strange', 'Что искать глазами', [
          'Первый просмотр нужен не ради красивого вывода. В строках можно заметить невозможные значения, неверный разделитель, числа как строки, лишнюю колонку индекса или категорию, написанную несколькими способами.',
          '`describe()` помогает увидеть подозрительные минимумы и максимумы, а `isna().sum()` показывает, где очистка понадобится почти наверняка. После такого осмотра следующий шаг становится предметным: не “почистить всё”, а разобраться с конкретными столбцами.',
        ], {
          callouts: [
            callout('Где используется', 'Перед очисткой данных, перед построением графиков, перед выбором признаков и перед любым обучением модели.', 'example'),
          ],
        }),
      ],
    ),
    theoryStep(
      'pandas-loading-readable-report',
      'Мини-отчёт после чтения',
      'Формируем привычку оставлять короткий вывод.',
      [
        section('report', 'Что сказать после первого осмотра', [
          'После первого осмотра полезно сформулировать короткий вывод: сколько строк и столбцов, какие типы данных требуют внимания, где есть пропуски, какие числовые диапазоны выглядят странно.',
          'Такой мини-отчёт дисциплинирует анализ. Он показывает, что вы не просто вызвали методы, а поняли, что делать дальше: выбирать признаки, чистить пропуски, исправлять типы или проверять выбросы.',
        ], {
          callouts: [
            callout('Что теперь умеем', 'Открывать таблицу как датасет для анализа: видеть строки, структуру, типы, пропуски и первые подозрительные места.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'pandas-read-quiz-checklist',
      'Первый чек-лист',
      'Проверяем порядок первичного осмотра.',
      singleQuiz(
        'quiz-pandas-read-checklist',
        'Первый осмотр',
        'pandas-read-inspect',
        'pandas-eda',
        'Какой набор лучше подходит для первого осмотра нового датасета?',
        [
          { id: 'a', text: '`head()`, `shape`, `info()`, `describe()`, `isna().sum()`' },
          { id: 'b', text: 'Только `groupby()`' },
          { id: 'c', text: 'Только `dropna()`' },
          { id: 'd', text: 'Сразу `pd.get_dummies()` без просмотра' },
        ],
        'a',
        'Этот набор быстро показывает строки, размер, типы, статистики и пропуски.',
      ),
    ),
    quizStep(
      'pandas-read-quiz-shape',
      'Размер таблицы',
      'Проверяем выбор инструмента.',
      singleQuiz(
        'quiz-pandas-read-shape',
        'Размер',
        'pandas-read-inspect',
        'pandas-eda',
        'Что быстрее всего покажет число строк и столбцов?',
        [
          { id: 'a', text: '`df.shape`' },
          { id: 'b', text: '`df.dtypes`' },
          { id: 'c', text: '`df.columns`' },
          { id: 'd', text: '`df.sample()`' },
        ],
        'a',
        '`shape` возвращает пару: `(число строк, число столбцов)`.',
      ),
    ),
    practiceStep(
      'pandas-read-practice',
      'Первый отчёт по таблице',
      'Выводим размер и пропуски.',
      makeStdinTask(
        'task-pandas-first-report',
        'Первый отчёт по таблице',
        'Создайте DataFrame из готового словаря в starter code, затем выведите `shape` и количество пропусков по столбцам.',
        `
          import pandas as pd

          data = {
              "area": [35, 52, 80],
              "rooms": [1, 2, 3],
              "price": [8.2, 9.1, None],
          }

          # TODO: создайте DataFrame

          # TODO: выведите shape

          # TODO: выведите количество пропусков по столбцам
        `,
        [
          { id: 's1', description: 'Размер и пропуски', expectedOutput: '(3, 3)\narea     0\nrooms    0\nprice    1\ndtype: int64' },
        ],
        [
          { id: 'h1', description: 'Повторная проверка', expectedOutput: '(3, 3)\narea     0\nrooms    0\nprice    1\ndtype: int64' },
        ],
        `
          import pandas as pd

          data = {
              "area": [35, 52, 80],
              "rooms": [1, 2, 3],
              "price": [8.2, 9.1, None],
          }

          df = pd.DataFrame(data)
          print(df.shape)
          print(df.isna().sum())
        `,
      ),
    ),
  ],
)
