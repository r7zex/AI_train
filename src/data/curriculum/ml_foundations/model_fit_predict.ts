import type { FlowTopic } from '../../aiCurriculumTypes'
import { callout, code, makeStdinTask, mlFoundationsTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicMlModelFitPredict: FlowTopic = mlFoundationsTopic(
  'ml-foundations-model-fit-predict',
  '4.2 Модель, обучение и предсказание',
  2,
  'Продолжаем синтетический пример оттока: различаем объект-оцениватель и обученную модель, разделяем fit и predict и проверяем контракт признаков.',
  'До fit объект хранит настройки алгоритма; после fit обученная модель хранит оценённые по training data параметры или правила и применяет их к X_new.',
  ['model', 'параметры модели', 'обучение', 'предсказание', 'fit', 'predict', 'X_train', 'y_train', 'X_new', 'y_pred'],
  ['X_train + y_train → fit → trained model', 'X_new → predict → y_pred'],
  ['При fit есть правильные ответы; при predict правильный ответ неизвестен; модель работает с признаками, а не с человеческим пониманием объекта.'],
  [
    theoryStep(
      'ml-foundations-model-fit-predict-model',
      'Оцениватель до fit и модель после fit',
      'Один программный объект проходит два состояния: настроенный алгоритм до обучения и обученная модель после него.',
      [
        section('definition', 'Вычислительное правило для прогноза', [
          '**Объект-оцениватель (estimator)** до обучения хранит выбранный алгоритм и его настройки, но ещё не содержит параметров или правил, оценённых по конкретным обучающим строкам.',
          '**Обученная модель (fitted model)** после обучения хранит полученное по данным состояние и вычисляет прогноз из признаков нового объекта. Это вычислительное преобразование, а не человеческое понимание клиента.',
        ], {
          callouts: [
            callout('Точно', 'До `fit`: алгоритм + настройки. После `fit`: тот же объект + параметры, правила или сводки, оценённые по training data (обучающим данным).', 'schema'),
          ],
        }),
        section('state-before-after', 'Что именно появляется после fit', [
          'В сквозном примере используется простая сравнительная стратегия: всегда возвращать самый частый класс из пяти обучающих ответов. До `fit` задана только настройка `strategy="most_frequent"`. После `fit` классы хранятся в порядке `[да, нет]`, соответствующие доли равны `[2/5, 3/5]`, а итоговое правило возвращает «нет».',
          'Другие алгоритмы сохраняют другое состояние: линейная модель - коэффициенты, дерево - условия разбиений и ответы листьев. Поэтому нельзя определять обучение одной конкретной процедурой, общей для всех алгоритмов.',
        ], {
          table: {
            headers: ['Состояние', 'Что уже есть', 'Чего ещё нет / что появилось'],
            rows: [
              ['estimator до `fit`', 'алгоритм и `strategy="most_frequent"`', 'нет `classes_` и `class_prior_`'],
              ['model после `fit`', 'те же настройки', '`classes_ = [да, нет]`, доли `[2/5, 3/5]`, правило «нет»'],
            ],
          },
        }),
      ],
    ),
    theoryStep(
      'ml-foundations-model-fit-predict-fit',
      'Что означает fit',
      'Fit оценивает по X_train и y_train то внутреннее состояние, которое предусмотрено выбранным алгоритмом.',
      [
        section('training', 'Оценивание параметров, правил или сводок', [
          '**Обучение (`fit`)** - оценивание параметров, правил или сводных значений по обучающим данным (`X_train`, `y_train`). Метод меняет состояние estimator и возвращает обученную model.',
          'Фраза «делает пробные прогнозы и исправляет себя» не является универсальным определением. Некоторые линейные методы действительно оптимизируют коэффициенты итеративно, дерево выбирает разбиения, а простая сравнительная модель может лишь посчитать частоты классов.',
        ], {
          table: {
            headers: ['Алгоритм', 'Что оценивает fit'],
            rows: [
              ['линейная модель', 'коэффициенты и свободный член'],
              ['дерево решений', 'признак и порог в узлах, ответы в листьях'],
              ['most-frequent dummy', 'частоты классов и самый частый класс'],
            ],
          },
          codeExamples: [
            code('python', `
              estimator.fit(X_train, y_train)
            `, undefined, '`fit` получает и признаки, и ответы; конкретный результат зависит от алгоритма.'),
          ],
        }),
        section('parameters-hyperparameters', 'Параметр и гиперпараметр - коротко', [
          '**Гиперпараметр (hyperparameter)** задают до `fit`: в примере это `strategy="most_frequent"`. **Параметр модели (model parameter)** или обученное состояние получают из данных: при `classes_ = [да, нет]` это доли `class_prior_ = [2/5, 3/5]`.',
          'Подробно - в теме 4.15. Сейчас достаточно различать настройку до обучения и результат оценивания по данным.',
        ]),
      ],
    ),
    theoryStep(
      'ml-foundations-model-fit-predict-predict',
      'Что означает predict',
      'Predict применяет зафиксированное после fit состояние к новым строкам и возвращает по одному результату на строку.',
      [
        section('prediction', 'Новый объект без правильного ответа', [
          '**Получение прогноза (`predict`)** принимает только признаки новых объектов (`X_new`) и возвращает предсказанные ответы (`y_pred`). Обычный `predict` не получает `y_train` и не изменяет обученные параметры.',
          'Для клиента C106 доступны `[24, "pro", 3]`. Правильный `churn_after_30d` в момент решения неизвестен. Обученная на строках C101-C105 простая модель возвращает класс «нет». Этот одиночный прогноз показывает механику inference и не является test-оценкой качества модели.',
        ], {
          callouts: [
            callout('Форма результата', 'Если `X_new` содержит `m` объектов, то `y_pred` содержит `m` значений. Здесь `X_new.shape = (1, 3)`, поэтому `y_pred.shape = (1,)`.', 'remember'),
            callout('Не путать с проверкой', 'C105 и C106 относятся к одной группе H3. Поэтому C106 нельзя использовать как независимый test-объект после обучения на C105. Здесь он нужен только для демонстрации вызова `predict`; честное качество оценивают на отдельном разбиении без пересечения групп.', 'important'),
          ],
        }),
        section('same-data-code', 'Те же шесть синтетических строк в коде', [
          'Первые пять строк образуют training data (обучающие данные), шестая строка C106 - `X_new` для демонстрации inference, а не test. Список `feature_cols` один и тот же при обучении и прогнозе.',
        ], {
          codeExamples: [
            code('python', `
              import pandas as pd
              from sklearn.dummy import DummyClassifier
              from sklearn.pipeline import Pipeline

              df = pd.DataFrame({
                  "client_id": ["C101", "C102", "C103", "C104", "C105", "C106"],
                  "household_group": ["H1", "H1", "H2", "H2", "H3", "H3"],
                  "days_since_login": [2, 18, 5, 31, 9, 24],
                  "tariff": ["pro", "basic", "pro", "basic", "basic", "pro"],
                  "support_tickets": [0, 2, 1, 4, 0, 3],
                  "churn_after_30d": ["нет", "да", "нет", "да", "нет", "да"],
              })

              feature_cols = ["days_since_login", "tariff", "support_tickets"]
              X_train = df.loc[:4, feature_cols]
              y_train = df.loc[:4, "churn_after_30d"]
              X_new = df.loc[[5], feature_cols]

              estimator = Pipeline([
                  ("classifier", DummyClassifier(strategy="most_frequent")),
              ])
              print(hasattr(estimator.named_steps["classifier"], "classes_"))

              model = estimator.fit(X_train, y_train)
              fitted = model.named_steps["classifier"]
              y_pred = model.predict(X_new)

              print(fitted.classes_, fitted.class_prior_)
              print(X_train.shape, y_train.shape, X_new.shape, y_pred.shape)
              print(y_pred)
            `, `
              False
              ['да' 'нет'] [0.4 0.6]
              (5, 3) (5,) (1, 3) (1,)
              ['нет']
            `, 'До fit атрибута `classes_` нет; после fit сохранены доли классов. Для одной строки C106 получено одно значение.'),
          ],
        }),
        section('output-kinds', 'Число, класс и вероятность - разные результаты', [
          '`predict` в регрессии возвращает числовой прогноз, а в классификации - метку класса. `predict_proba` в классификаторах, которые его поддерживают, возвращает вероятности классов; подробнее о вероятностях будет в теме 4.11.',
        ], {
          table: {
            headers: ['Задача / метод', 'Пример результата', 'Смысл'],
            rows: [
              ['регрессия, `predict`', '`10.8`', 'числовой прогноз, например цена'],
              ['классификация, `predict`', '`нет`', 'метка класса оттока для C106'],
              ['классификация, `predict_proba`', '`[0.4, 0.6]` для `[да, нет]`', 'вероятности двух классов'],
            ],
          },
        }),
      ],
    ),
    theoryStep(
      'ml-foundations-model-fit-predict-compare',
      'Два пути и один контракт признаков',
      'Training path оценивает состояние, inference path только применяет его; обе ветви обязаны одинаково понимать столбцы.',
      [
        section('two-paths', 'Training и inference не смешиваются', [
          '**Путь обучения (training path):** `X_train + y_train → fit → fitted model`. Здесь ответы известны и состояние estimator изменяется.',
          '**Путь применения (inference path):** `X_new → predict → y_pred`. Здесь `y` не подаётся, а параметры обученной модели при обычном `predict` остаются неизменными.',
        ], {
          callouts: [
            callout('Главное различие', 'Fit получает X и y и изменяет состояние. Predict получает только X и использует уже зафиксированное состояние.', 'schema'),
          ],
        }),
        section('feature-contract', 'Контракт между обучением и применением', [
          'В `X_new` должны быть **те же признаки, в том же порядке, в тех же единицах и после тех же преобразований**, что и в `X_train`. Иначе одна позиция начнёт означать другое измерение или модель не сможет обработать строку.',
        ], {
          table: {
            headers: ['Нарушение', 'Пример для C106', 'Результат'],
            rows: [
              ['missing column', 'нет `tariff`', 'не хватает обучающего признака'],
              ['extra column', 'добавлен `churn_after_30d`', 'появился лишний столбец и утечка target'],
              ['reordered column', '`support_tickets` поставлен перед `days_since_login`', 'массив без имён может перепутать смысл позиций'],
              ['другие единицы', 'давность передана в часах вместо дней', 'число имеет другой масштаб и смысл'],
            ],
          },
          callouts: [
            callout('Как предотвратить', 'Храните один `feature_cols`, выбирайте им столбцы из DataFrame и объединяйте преобразования с моделью в `Pipeline` (конвейер). Тогда при `predict` повторяется та же последовательность. Подробно - в теме 4.16.', 'important'),
          ],
        }),
      ],
    ),
    quizStep(
      'ml-foundations-model-fit-predict-quiz',
      'Разделить fit и predict',
      'Проверяем входы двух ветвей на строке C106.',
      singleQuiz(
        'quiz-ml-foundations-fit-predict',
        'fit и predict',
        'ml-foundations-model-fit-predict',
        'ml-foundations',
        'Модель уже обучена на клиентах C101-C105. Что нужно передать в `predict` для клиента C106?',
        [
          { id: 'a', text: 'только `X_new` с теми же тремя признаками' },
          { id: 'b', text: '`X_new` и будущий `churn_after_30d`' },
          { id: 'c', text: 'только `y_train`' },
          { id: 'd', text: 'новое значение гиперпараметра вместо признаков' },
        ],
        'a',
        '`predict` получает только признаки C106. Будущий target неизвестен, а обученные параметры при обычном прогнозе не меняются.',
      ),
    ),
    practiceStep(
      'ml-foundations-model-fit-predict-practice',
      'Разложить этапы обучения',
      'Выводим правильный порядок базовых этапов.',
      makeStdinTask(
        'task-ml-foundations-order-fit-predict',
        'Разложить этапы обучения',
        'На вход подаются четыре этапа в произвольном порядке. Выведите правильный порядок через пробел: сначала сбор данных, затем обучение, затем предсказание, затем оценка.',
        `
          # TODO: считайте строку с этапами

          # TODO: выведите этапы в правильном порядке
        `,
        [
          { id: 's1', description: 'Перемешанный порядок', input: 'predict_new collect_data evaluate fit_model', expectedOutput: 'collect_data fit_model predict_new evaluate' },
        ],
        [
          { id: 'h1', description: 'Другой порядок', input: 'evaluate fit_model collect_data predict_new', expectedOutput: 'collect_data fit_model predict_new evaluate' },
        ],
        `
          _stages = input().split()
          print("collect_data fit_model predict_new evaluate")
        `,
      ),
    ),
  ],
)
