import type { FlowTopic } from '../../aiCurriculumTypes'
import {
  callout,
  code,
  makeStdinTask,
  mlFoundationsTopic,
  practiceStep,
  quizStep,
  section,
  singleQuiz,
  theoryStep,
  theoryStepWithFormulas,
} from '../helpers'

export const topicMlTrainTestBaselineMetrics: FlowTopic = mlFoundationsTopic(
  'ml-foundations-train-test-baseline-metrics',
  '4.3 Честная проверка, простая модель и базовые метрики',
  3,
  'Разделяем роли данных, задаём простую сравнительную процедуру и читаем метрики регрессии и классификации на согласованных синтетических примерах.',
  'Сначала отделяют данные для обучения, выбора решения и финальной проверки, затем сравнивают простую процедуру и модель одной метрикой на одной и той же части данных.',
  ['train', 'validation', 'test', 'overfitting', 'baseline', 'DummyRegressor', 'DummyClassifier', 'metric', 'y_true', 'y_pred', 'MAE', 'MSE', 'RMSE', 'R²', 'accuracy', 'precision', 'recall', 'F1', 'TP', 'TN', 'FP', 'FN'],
  ['train → fit', 'validation → выбор решения', 'test → одна финальная оценка', 'MAE=sum|y_i-yhat_i|/n', 'RMSE=sqrt(MSE)'],
  [
    'обучающая часть (train) — данные для оценки параметров модели и обучаемых преобразований',
    'проверочная часть (validation) — данные для выбора модели, признаков, порога и других решений',
    'тестовая часть (test) — закрытые данные для одной финальной оценки выбранной процедуры',
    'простая сравнительная процедура (baseline) — заранее заданное простое правило, которое основная модель должна превзойти',
  ],
  [
    theoryStep(
      'ml-foundations-train-test-baseline-metrics-split',
      'Почему делят данные и какие роли получают части',
      'Сначала отделяем честную оценку от обучения, затем различаем train, validation и test.',
      [
        section('why', 'Почему проверка на знакомых строках нечестна', [
          'Если оценить модель на тех же строках, по которым она подбирала параметры, результат будет оптимистичным. Алгоритм мог запомнить детали и шум. Это переобучение (overfitting): высокое качество на знакомых данных не подтверждает качество на новых объектах.',
          'Один фиксированный split на обучающую и тестовую части допустим для первого учебного эксперимента, если результат test не влияет ни на одну следующую правку. Как только по оценке выбирают признаки, алгоритм, гиперпараметры или порог, test начинает участвовать в разработке и перестаёт быть независимой финальной проверкой.',
          'Для повторяющегося выбора используют отдельную проверочную часть (validation) или кросс-валидацию (cross-validation, CV) внутри train. Тестовую часть открывают после выбора всей процедуры. Если после просмотра test решение изменили, следующая честная оценка требует новых закрытых данных.',
        ], {
          callouts: [
            callout('Граница учебного примера', 'Один train/test split показывает механику первого фиксированного запуска. Рекомендуемая разработка отделяет validation или CV для выбора и сохраняет test до финала.', 'important'),
          ],
        }),
        section('roles', 'Роли train, validation и test: пример 60/20/20', [
          'В простом синтетическом примере из 100 независимых объектов 60 отправим в train, 20 — в validation и 20 — в test. Доли 60/20/20 нужны здесь для наглядности, а не как универсальное правило: реальные размеры зависят от объёма данных, редкости событий, групп, времени и требуемой точности оценки.',
          'Train содержит `X_train` и `y_train`: здесь выполняют `fit`. Validation не обучает параметры выбранного запуска, но её метрика помогает сравнивать варианты. Test не участвует ни в `fit`, ни в выборе; на нём один раз оценивают уже зафиксированную процедуру.',
        ], {
          table: {
            headers: ['Часть', 'Доля в примере', 'Роль', 'Что запрещено'],
            rows: [
              ['обучающая (train)', '60 объектов / 60%', 'обучить преобразования и модель', 'оценивать по ней качество на новых данных'],
              ['проверочная (validation)', '20 объектов / 20%', 'выбрать признаки, модель, настройки и порог', 'передавать её ответы в fit этого варианта'],
              ['тестовая (test)', '20 объектов / 20%', 'один раз проверить выбранную процедуру', 'принимать по ней решения разработки'],
            ],
          },
          codeExamples: [code('python', `import numpy as np
from sklearn.model_selection import train_test_split

X = np.arange(100).reshape(-1, 1)
y = np.array([0, 1] * 50)

X_train_val, X_test, y_train_val, y_test = train_test_split(
    X, y, test_size=0.20, stratify=y, random_state=42
)
X_train, X_validation, y_train, y_validation = train_test_split(
    X_train_val,
    y_train_val,
    test_size=0.25,
    stratify=y_train_val,
    random_state=42,
)

print(len(X_train), len(X_validation), len(X_test))`, `60 20 20`, 'Второй вызов откладывает 25% от оставшихся 80 объектов, то есть 20 объектов; test после первого вызова больше не участвует в выборе.')],
        }),
        section('leakage-boundary', 'Что можно делать до split, а что обучают только на train', [
          'Утечка предобработки (preprocessing leakage) возникает, когда операция оценивает статистики или структуру по validation/test, а затем использует результат при обучении. Среднее для заполнения пропусков, параметры масштабирования, словарь категорий, порог частоты и выбор признаков должны получать `fit` только на train; к validation/test применяют уже зафиксированное преобразование.',
          'Не вся обработка обучается по данным. Детерминированный парсинг даты, проверка формата файла, исправление заранее известного кода пропуска или перевод единиц по фиксированному правилу могут выполняться до split, если правило не выбиралось по целевой переменной или распределению всего набора.',
          'Фраза «вообще всё только после split» слишком груба. Точная граница проходит между заранее заданной технической операцией и процедурой, которая извлекает статистики, категории, признаки или решения из наблюдаемых данных.',
        ], {
          table: {
            headers: ['Операция', 'Когда допустима', 'Почему'],
            rows: [
              ['разобрать строку даты по заданному формату', 'до split', 'правило не оценивается по набору'],
              ['перевести сантиметры в метры делением на 100', 'до split', 'коэффициент задан заранее'],
              ['заполнить пропуски медианой', 'fit только на train', 'медиана — обучаемая статистика'],
              ['выбрать признаки по связи с y', 'только внутри train / CV', 'выбор использует ответы'],
            ],
          },
        }),
        section('reproducibility', 'random_state повторяет split, но не делает его честным', [
          'Зерно случайности (`random_state`) фиксирует конкретное псевдослучайное разбиение и помогает воспроизвести сравнение. Оно не проверяет корректность единицы наблюдения и не устраняет утечку.',
          'Контрпример по группам: два визита одного пациента могут при любом фиксированном seed попасть в разные части, и модель узнает особенности пациента. Контрпример по времени: случайный split может отправить будущие записи в train, а прошлые — в test. Результат будет воспроизводимо смещённым.',
          'До выбора функции разбиения определяют зависимости между объектами. Для связанных групп применяют групповое разбиение, для прогноза будущего — временное; `random_state=42` не заменяет это решение.',
        ], {
          callouts: [callout('Воспроизводимость не равна честности', 'Seed отвечает на вопрос «получим ли мы те же строки?», а стратегия split — на вопрос «имитируют ли эти строки реальные новые случаи?».', 'remember')],
        }),
      ],
    ),
    theoryStep(
      'ml-foundations-train-test-baseline-metrics-baseline',
      'Простая сравнительная процедура (baseline)',
      'Заранее задаём простое правило, обучаем его только на y_train и сравниваем с моделью в одинаковых условиях.',
      [
        section('definition', 'Baseline — процедура, а не одинокое число', [
          'Простая сравнительная процедура (baseline) — заранее заданное правило, которое строит прогнозы без сложной зависимости от признаков. В регрессии это может быть среднее или медиана `y_train`, в классификации — самый частый класс или доля положительного класса из `y_train`.',
          'Baseline проходит тот же протокол, что основная модель: получает сведения только из train, прогнозирует те же validation- или test-объекты и оценивается той же метрикой. Среднее по `y_test` или частый класс по всему набору подсматривают ответы и не являются честным baseline.',
          'В scikit-learn намеренно простые модели называются `DummyRegressor` и `DummyClassifier`. Слово dummy здесь означает техническую сравнительную модель; её задача — показать, извлекла ли основная модель полезный сигнал сверх простого правила.',
        ], {
          table: {
            headers: ['Задача', 'Заранее заданное правило', 'Что разрешено запомнить при fit'],
            rows: [
              ['числовой прогноз', 'всегда выдавать среднее', 'только среднее `y_train`'],
              ['устойчивый числовой прогноз', 'всегда выдавать медиану', 'только медиану `y_train`'],
              ['предсказание класса', 'всегда выдавать частый класс', 'только частоты классов `y_train`'],
            ],
          },
        }),
        section('same-part-comparison', 'Baseline и модель на одной validation-части', [
          'В согласованном синтетическом примере обучающие ответы равны [8, 10, 12, 14, 18, 22], поэтому средний baseline после `fit` равен 14. На одной validation-части `y_true=[10, 12, 20]` baseline предсказывает [14, 14, 14], а сохранённые прогнозы модели равны `y_pred=[11, 10, 16]`.',
          'Средняя абсолютная ошибка baseline равна 4.00 единицы, у модели — 2.33 единицы. Разность `model − baseline` равна −1.67 единицы: для ошибки отрицательная delta означает улучшение. Число 2.33 без одновременных 4.00 и −1.67 не позволило бы сделать этот вывод.',
        ], {
          table: {
            headers: ['Процедура', 'Часть данных', 'Метрика', 'Score', 'Delta к baseline', 'Интерпретация'],
            rows: [
              ['среднее `y_train`', 'одна validation', 'MAE', '4.00', '0.00', 'точка сравнения'],
              ['основная модель', 'та же validation', 'MAE', '2.33', '−1.67', 'ошибка ниже baseline на 1.67 единицы'],
            ],
          },
          codeExamples: [code('python', `import numpy as np
from sklearn.dummy import DummyRegressor
from sklearn.metrics import mean_absolute_error

X_train = np.zeros((6, 1))
y_train = np.array([8, 10, 12, 14, 18, 22])
X_validation = np.zeros((3, 1))
y_true = np.array([10, 12, 20])
y_pred = np.array([11, 10, 16])

baseline = DummyRegressor(strategy="mean")
baseline.fit(X_train, y_train)
baseline_pred = baseline.predict(X_validation)

baseline_mae = mean_absolute_error(y_true, baseline_pred)
model_mae = mean_absolute_error(y_true, y_pred)
delta = model_mae - baseline_mae
print(f"baseline MAE={baseline_mae:.2f}")
print(f"model MAE={model_mae:.2f}")
print(f"delta={delta:.2f}")`, `baseline MAE=4.00
model MAE=2.33
delta=-1.67`, 'DummyRegressor получает среднее только из y_train; оба набора прогнозов оцениваются по одной и той же y_true validation-части.')],
          callouts: [callout('Правило вывода', 'Сначала назовите baseline, часть данных и одну метрику; затем покажите два score, delta и смысл направления изменения.', 'summary')],
        }),
      ],
    ),
    theoryStepWithFormulas(
      'ml-foundations-train-test-baseline-metrics-regression',
      'Метрики регрессии: один пример, разные вопросы',
      'Считаем MAE, MSE, RMSE и R² по одним y_true=[10, 12, 20] и y_pred=[11, 10, 16].',
      [
        section('errors', 'Ошибки трёх validation-прогнозов', [
          'Во всём разделе используется один набор: правильные ответы `y_true=[10, 12, 20]`, прогнозы модели `y_pred=[11, 10, 16]`. Остатки `y_i−ŷ_i` равны [−1, 2, 4], их модули — [1, 2, 4], квадраты — [1, 4, 16].',
          'Средняя абсолютная ошибка (MAE) равна 7/3≈2.333 и измеряется в единицах target. Средняя квадратичная ошибка (MSE) равна 21/3=7.000 квадратных единиц. Корень средней квадратичной ошибки (RMSE) равен √7≈2.646 и снова измеряется в единицах target.',
          'Квадрат усиливает вклад большого промаха, поэтому MSE и RMSE чувствительнее к выбросам, чем MAE. Если только последний прогноз заменить с 16 на 0, модули ошибок станут [1, 2, 20]: MAE вырастет до 7.67, а RMSE — до 11.62. Это отдельная проверка чувствительности, а не новый основной набор.',
        ], {
          table: {
            headers: ['Метрика', 'Значение на основном примере', 'Единицы', 'Реакция на крупный промах'],
            rows: [
              ['MAE', '2.333', 'единицы target', 'растёт линейно'],
              ['MSE', '7.000', 'квадратные единицы target', 'растёт квадратично'],
              ['RMSE', '2.646', 'единицы target', 'растёт сильнее MAE из-за квадрата'],
              ['R²', '0.625', 'без единиц', 'сравнивает сумму квадратов с прогнозом средним'],
            ],
          },
        }),
        section('r2-range', 'Как читать R² и его особые случаи', [
          'Коэффициент детерминации R² сравнивает сумму квадратов ошибок модели с правилом «всегда прогнозировать среднее `y_true`». В основном примере среднее равно 14, сумма квадратов отклонений от среднего равна 56, поэтому R²=1−21/56=0.625.',
          'Максимум R² равен 1 для точного прогноза. R²=0 соответствует по сумме квадратов прогнозу средним, а нижней границы нет. Например, постоянный прогноз [20, 20, 20] на тех же ответах даёт R²=1−164/56≈−1.93: он хуже среднего baseline.',
          'Если все `y_true` одинаковы, знаменатель формулы равен нулю и обычное отношение не определено. Scikit-learn по умолчанию делает результат конечным: возвращает 1.0 для точного постоянного прогноза и 0.0 для неточного; при `force_finite=False` виден исходный особый результат.',
          'R² не является процентом точности и не имеет единиц target. Для понятного отчёта его дополняют MAE или RMSE на той же части данных.',
        ], {
          codeExamples: [code('python', `import numpy as np
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

y_true = np.array([10, 12, 20])
y_pred = np.array([11, 10, 16])

mae = mean_absolute_error(y_true, y_pred)
mse = mean_squared_error(y_true, y_pred)
rmse = np.sqrt(mse)
r2 = r2_score(y_true, y_pred)
print(f"MAE={mae:.3f}; MSE={mse:.3f}; RMSE={rmse:.3f}; R²={r2:.3f}")`, `MAE=2.333; MSE=7.000; RMSE=2.646; R²=0.625`, 'Код, текст, карточки формул, quiz и рисунок используют одни y_true и y_pred; округление выполняется только при выводе.')],
        }),
      ],
      [
        { label: 'Средняя абсолютная ошибка (MAE)', expression: String.raw`MAE=\frac{1}{n}\sum_{i=1}^{n}|y_i-\hat{y}_i|`, meaning: 'Среднее расстояние между правильным ответом и прогнозом без учёта направления.', notation: ['n — число объектов', 'y_i — правильный ответ', String.raw`\hat{y}_i — прогноз`], example: { title: 'y_true=[10, 12, 20], y_pred=[11, 10, 16]', steps: ['Остатки: −1, 2, 4.', 'Модули: 1, 2, 4.', '(1+2+4)/3 = 7/3 ≈ 2.333.'], conclusion: 'MAE≈2.333 единицы target.' } },
        { label: 'Средняя квадратичная ошибка (MSE)', expression: String.raw`MSE=\frac{1}{n}\sum_{i=1}^{n}(y_i-\hat{y}_i)^2`, meaning: 'Квадраты не дают ошибкам взаимно уничтожиться и усиливают крупные промахи.', notation: ['n — число объектов', String.raw`y_i-\hat{y}_i — остаток`], example: { title: 'Те же y_true и y_pred', steps: ['Остатки: −1, 2, 4.', 'Квадраты: 1, 4, 16.', '(1+4+16)/3 = 7.000.'], conclusion: 'MSE=7.000 квадратных единиц target.' } },
        { label: 'Корень средней квадратичной ошибки (RMSE)', expression: String.raw`RMSE=\sqrt{MSE}`, meaning: 'Корень возвращает квадратичную ошибку в исходные единицы target.', notation: ['MSE — средняя квадратичная ошибка'], example: { title: 'Те же y_true и y_pred', steps: ['MSE = 7.000.', 'RMSE = √7.', '√7 ≈ 2.646.'], conclusion: 'RMSE≈2.646 единицы target.' } },
        { label: 'Коэффициент детерминации (R²)', expression: String.raw`R^2=1-\frac{\sum_i(y_i-\hat{y}_i)^2}{\sum_i(y_i-\bar{y})^2}`, meaning: 'Сравнивает сумму квадратов ошибок модели с суммой квадратов ошибок прогноза средним.', notation: [String.raw`\bar{y} — среднее y_true`, 'числитель — сумма квадратов ошибок модели', 'знаменатель — сумма квадратов ошибок прогноза средним'], example: { title: 'Те же y_true и y_pred', steps: ['Среднее y_true: 14.', 'Числитель: 1+4+16=21; знаменатель: 16+4+36=56.', 'R² = 1−21/56 = 0.625.'], conclusion: 'R²=0.625: модель лучше прогноза средним по сумме квадратов.' } },
      ],
    ),
    theoryStepWithFormulas(
      'ml-foundations-train-test-baseline-metrics-classification',
      'Метрики классификации: от четырёх клеток к порогу',
      'Сначала фиксируем положительный класс и ориентацию матрицы, затем определяем TP/TN/FP/FN и только после этого считаем метрики.',
      [
        section('confusion', 'Положительный класс и ориентация матрицы', [
          'Рассмотрим явно синтетический скрининг 100 объектов, не реальные клинические данные. Положительный класс 1 означает «событие есть», отрицательный класс 0 — «события нет». В матрице строки — правильный ответ, столбцы — прогноз модели.',
          'Истинно отрицательный исход (TN) находится в строке 0 и столбце 0: события нет и прогноз 0. Ложноположительный (FP) — строка 0, столбец 1: ложная тревога. Ложноотрицательный (FN) — строка 1, столбец 0: пропуск события. Истинно положительный (TP) — строка 1, столбец 1: событие найдено.',
          'В примере TN=72, FP=18, FN=2, TP=8. Эти четыре определения и ориентацию нужно проверить до любой формулы: перестановка класса или осей меняет смысл результата.',
        ], {
          table: {
            headers: ['Правильный класс (строка)', 'Прогноз (столбец)', 'Счётчик', 'Число', 'Смысл'],
            rows: [
              ['0: события нет', '0: события нет', 'TN', '72', 'правильно исключили'],
              ['0: события нет', '1: событие есть', 'FP', '18', 'ложная тревога'],
              ['1: событие есть', '0: события нет', 'FN', '2', 'пропуск'],
              ['1: событие есть', '1: событие есть', 'TP', '8', 'правильно нашли'],
            ],
          },
          callouts: [callout('Порядок чтения', '1) назовите positive class; 2) подпишите строки как истину и столбцы как прогноз; 3) найдите четыре клетки; 4) только затем считайте метрики.', 'schema')],
        }),
        section('classification-metrics', 'Accuracy, precision, recall и точный F1', [
          'Доля правильных ответов (accuracy) равна (TP+TN)/N=(8+72)/100=0.800. Она объединяет оба класса и может скрывать провал на редком положительном классе.',
          'Точность положительных прогнозов (precision) равна TP/(TP+FP)=8/26≈0.308: столько тревог подтвердилось. Полнота (recall) равна TP/(TP+FN)=8/10=0.800: столько реальных положительных случаев найдено.',
          'F1-мера считается из исходных счётчиков без промежуточного округления: F1=2TP/(2TP+FP+FN)=16/(16+18+2)=16/36=4/9≈0.444. Подстановка округлённых 0.31 и 0.80 даёт искажённый промежуточный результат, поэтому округляют только финальное число.',
          'Порог превращает вероятность в класс. При снижении порога положительных прогнозов обычно становится больше: recall может вырасти вместе с FP. При повышении порога тревог обычно меньше: precision может вырасти, но FN тоже может стать больше. Порог выбирают на validation по цене ошибок, а не на test.',
        ], {
          table: {
            headers: ['Метрика', 'Точный расчёт', 'Значение', 'Практический вопрос'],
            rows: [
              ['accuracy', '(8+72)/100', '0.800', 'какая доля всех ответов верна?'],
              ['precision', '8/(8+18)', '0.308', 'какая доля тревог подтвердилась?'],
              ['recall', '8/(8+2)', '0.800', 'какая доля событий найдена?'],
              ['F1', '16/(16+18+2)', '0.444', 'каков баланс precision и recall без TN?'],
            ],
          },
          codeExamples: [code('python', `from sklearn.metrics import confusion_matrix, precision_score, recall_score, f1_score

y_true = [1] * 10 + [0] * 90
y_pred = [1] * 8 + [0] * 2 + [1] * 18 + [0] * 72

tn, fp, fn, tp = confusion_matrix(y_true, y_pred, labels=[0, 1]).ravel()
precision = precision_score(y_true, y_pred)
recall = recall_score(y_true, y_pred)
f1 = f1_score(y_true, y_pred)

print(f"TN={tn}, FP={fp}, FN={fn}, TP={tp}")
print(f"precision={precision:.3f}; recall={recall:.3f}; F1={f1:.3f}")`, `TN=72, FP=18, FN=2, TP=8
precision=0.308; recall=0.800; F1=0.444`, 'labels=[0, 1] закрепляет ориентацию матрицы; все метрики получают исходные y_true и y_pred и округляются только при печати.')],
          callouts: [callout('Метрика следует из решения', 'Если дороже пропуск, следят за recall и FN; если дороже лишнее действие, следят за precision и FP. F1 не кодирует стоимость и не заменяет матрицу ошибок.', 'important')],
        }),
      ],
      [
        { label: 'Точность положительных прогнозов (precision)', expression: String.raw`precision=\frac{TP}{TP+FP}`, meaning: 'Доля подтверждённых случаев среди всех положительных прогнозов.', notation: ['TP — правильно найденные положительные', 'FP — ложные тревоги'], example: { title: 'TP=8, FP=18', steps: ['Положительных прогнозов: 8+18=26.', 'Подтвердились 8.', '8/26 = 4/13 ≈ 0.308.'], conclusion: 'Подтвердилось около 30.8% тревог.' } },
        { label: 'Полнота (recall)', expression: String.raw`recall=\frac{TP}{TP+FN}`, meaning: 'Доля найденных случаев среди всех действительно положительных.', notation: ['TP — найденные случаи', 'FN — пропущенные случаи'], example: { title: 'TP=8, FN=2', steps: ['Реальных положительных: 8+2=10.', 'Найдены 8.', '8/10 = 0.800.'], conclusion: 'Найдено 80.0% положительных случаев.' } },
        { label: 'F1-мера', expression: String.raw`F1=2\frac{precision\cdot recall}{precision+recall}=\frac{2TP}{2TP+FP+FN}`, meaning: 'Гармоническое среднее precision и recall; прямая форма по счётчикам исключает двойное округление.', notation: ['TP — правильно найденные положительные', 'FP — ложные тревоги', 'FN — пропуски'], example: { title: 'TP=8, FP=18, FN=2', steps: ['Удвоенный TP: 2×8=16.', 'Знаменатель: 16+18+2=36.', 'F1=16/36=4/9≈0.444.'], conclusion: 'F1≈0.444 после единственного финального округления.' } },
      ],
    ),
    quizStep(
      'ml-foundations-train-test-baseline-metrics-quiz-baseline',
      'Проверка: простая сравнительная процедура',
      'Проверяем, что baseline обучается только по train и сравнивается в одинаковых условиях.',
      singleQuiz(
        'quiz-ml-foundations-baseline',
        'Простая сравнительная процедура',
        'ml-foundations-train-test-baseline-metrics',
        'ml-foundations',
        'Как правильно построить baseline медианного прогноза?',
        [
          { id: 'a', text: 'Вычислить медиану `y_train` и выдавать её для всех объектов validation/test' },
          { id: 'b', text: 'Вычислить медиану по `y_test`' },
          { id: 'c', text: 'Добавить target тестовых объектов в признаки' },
          { id: 'd', text: 'Сравнить baseline на validation, а модель только на train' },
        ],
        'a',
        'Медиана — обучаемая статистика baseline, поэтому её получают только из y_train. Обе процедуры оценивают одной метрикой на одной части данных.',
      ),
    ),
    quizStep(
      'ml-foundations-train-test-baseline-metrics-quiz-metric',
      'Проверка: согласованный пример регрессии',
      'Повторяем расчёт по тем же y_true и y_pred, что в тексте, коде, формулах и рисунке.',
      singleQuiz(
        'quiz-ml-foundations-metric',
        'Средняя квадратичная ошибка',
        'ml-foundations-train-test-baseline-metrics',
        'ml-foundations',
        'Для `y_true=[10, 12, 20]` и `y_pred=[11, 10, 16]` квадраты ошибок равны [1, 4, 16]. Чему равна MSE?',
        [
          { id: 'a', text: '7.00' },
          { id: 'b', text: '2.33' },
          { id: 'c', text: '2.65' },
          { id: 'd', text: '21.00' },
        ],
        'a',
        'MSE — среднее квадратов: (1+4+16)/3=21/3=7.00 квадратных единиц target.',
      ),
    ),
    practiceStep(
      'ml-foundations-train-test-baseline-metrics-practice',
      'Реализовать baseline без библиотеки',
      'Считаем простое регрессионное предсказание только по обучающим ответам.',
      makeStdinTask(
        'task-ml-foundations-regression-baseline',
        'Среднее обучающих ответов',
        'На вход подаётся список значений `y_train`. Выведите предсказание простой сравнительной процедуры — среднее значение, округлённое вниз до целого.',
        `values = list(map(float, input().split()))
# TODO: вычислите среднее только по полученным обучающим ответам
# TODO: подготовьте ответ`,
        [{ id: 's1', description: 'Три цены', input: '3000000 4000000 5000000', expectedOutput: '4000000' }],
        [{ id: 'h1', description: 'Четыре значения', input: '100 200 300 400', expectedOutput: '250' }, { id: 'h2', description: 'Неровное среднее', input: '1 2 2', expectedOutput: '1' }],
        `values = list(map(float, input().split()))
baseline = sum(values) / len(values)
print(int(baseline))`,
        [
          'Считайте только переданные обучающие ответы.',
          'Baseline выдаёт одно и то же число для каждого нового объекта.',
          'Округлите результат вниз преобразованием в int.',
        ],
      ),
    ),
  ],
)
