import type { FlowTopic } from '../../aiCurriculumTypes'
import { callout, code, makeStdinTask, mlFoundationsTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicMlDataTarget: FlowTopic = mlFoundationsTopic(
  'ml-foundations-data-target',
  '4.1 Данные, признаки и целевая переменная',
  1,
  'Разбираем на одной синтетической таблице единицу прогноза, роли столбцов, X, y, момент прогноза и четыре источника утечки.',
  'Строка описывает единицу прогноза, X содержит только допустимые признаки, а y - ответ, который станет известен после момента решения.',
  ['объект', 'наблюдение', 'dataset', 'feature', 'target', 'X', 'y', 'leakage'],
  ['X = features', 'y = target'],
  ['Признак допустим, только если он был доступен, одинаково измерен и разрешён в момент реального прогноза.'],
  [
    theoryStep(
      'ml-foundations-data-target-object',
      'Строка, столбец и единица прогноза',
      'Сначала формулируют вопрос, затем определяют, что означает одна строка и какую роль играет каждый столбец.',
      [
        section('synthetic-client-table', 'Одна таблица - один вопрос', [
          'Ниже приведён **явно синтетический набор данных (dataset)** для учебной задачи: по сведениям на дату решения предсказать, уйдёт ли клиент в следующие 30 дней. Числа придуманы для объяснения и не описывают реальную компанию.',
          '**Одна строка - единица прогноза:** один клиент в момент решения. **Столбцы - доступные сведения** об этом клиенте. **Целевая переменная (target), или метка (label), - ответ** `churn_after_30d`: ушёл ли клиент в следующие 30 дней.',
        ], {
          table: {
            headers: ['ID клиента', 'Группа', 'Дней без входа', 'Тариф', 'Обращения', 'Отток через 30 дней'],
            rows: [
              ['C101', 'H1', '2', 'pro', '0', 'нет'],
              ['C102', 'H1', '18', 'basic', '2', 'да'],
              ['C103', 'H2', '5', 'pro', '1', 'нет'],
              ['C104', 'H2', '31', 'basic', '4', 'да'],
              ['C105', 'H3', '9', 'basic', '0', 'нет'],
              ['C106', 'H3', '24', 'pro', '3', 'да'],
            ],
          },
          callouts: [
            callout('Учебные данные', 'Все шесть строк синтетические. Момент решения одинаков: начало 30-дневного окна наблюдения.', 'important'),
          ],
        }),
        section('terms', 'Не смешивать объект, наблюдение, признак и ответ', [
          '**Объект (object), пример (example) или наблюдение (observation)** - единица, для которой строится один прогноз. Конкретный смысл задаёт исследовательский вопрос, а не внешний вид таблицы.',
          '**Признак (feature)** - входное свойство объекта, допустимое в момент прогноза. **Целевая переменная (target), или метка (label)** - правильный ответ в исторических строках и неизвестный ответ для нового объекта.',
        ], {
          table: {
            headers: ['Исследовательский вопрос', 'Корректная единица одной строки', 'Почему именно она'],
            rows: [
              ['Какова цена объявления?', 'одна квартира', 'нужен один прогноз цены для квартиры'],
              ['Уйдёт ли клиент за 30 дней?', 'один клиент на дату решения', 'нужен один прогноз оттока для клиента'],
              ['Будет ли повторная госпитализация?', 'один завершённый визит пациента', 'у одного пациента может быть несколько отдельных визитов'],
            ],
          },
        }),
        section('column-roles', 'Роль каждого столбца', [
          'Название столбца само по себе не делает его признаком. Идентификатор нужен для связи записей, групповая колонка - для распознавания связанных объектов, признаки поступают в модель, а target хранит ответ.',
          'В основном примере используются только перечисленные ниже английские имена. Это не универсальный словарь: русский смысл указан рядом с каждым рабочим столбцом.',
        ], {
          table: {
            headers: ['Столбец', 'Русский смысл', 'Роль', 'Попадает в X?'],
            rows: [
              ['`client_id`', 'идентификатор клиента', 'id', 'нет'],
              ['`household_group`', 'связанная группа клиентов', 'group', 'нет'],
              ['`days_since_login`', 'дней с последнего входа', 'feature', 'да'],
              ['`tariff`', 'тариф на дату решения', 'feature', 'да'],
              ['`support_tickets`', 'обращения до даты решения', 'feature', 'да'],
              ['`churn_after_30d`', 'отток в следующие 30 дней', 'target', 'нет'],
            ],
          },
          callouts: [
            callout('ID и группы', '`client_id` и `household_group` помогают находить записи и связанные строки, но не становятся predictors (предикторами) автоматически.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'ml-foundations-data-target-x-y',
      'Физически отделить X от y',
      'X сохраняет строки допустимых признаков, а y - ровно один ответ для каждой из тех же строк.',
      [
        section('split-same-rows', 'Шесть строк превращаются в X и y', [
          'Из исходной `DataFrame` (таблицы pandas) выбираем три признака в `X`, а целевой столбец - в `y`. Идентификатор, группа и target в `X` не входят. Порядок шести строк сохраняется, поэтому строке C101 в `X` соответствует первый ответ «нет» в `y`, строке C106 - шестой ответ «да».',
          '`X.shape = (n, p)` означает `n` строк и `p` столбцов признаков. Здесь `n = 6`, `p = 3`, поэтому `X.shape = (6, 3)`. `y.shape = (n,)` означает одномерную последовательность из `n` ответов; здесь `y.shape = (6,)`.',
        ], {
          codeExamples: [
            code('python', `
              import pandas as pd

              df = pd.DataFrame({
                  "client_id": ["C101", "C102", "C103", "C104", "C105", "C106"],
                  "household_group": ["H1", "H1", "H2", "H2", "H3", "H3"],
                  "days_since_login": [2, 18, 5, 31, 9, 24],
                  "tariff": ["pro", "basic", "pro", "basic", "basic", "pro"],
                  "support_tickets": [0, 2, 1, 4, 0, 3],
                  "churn_after_30d": ["нет", "да", "нет", "да", "нет", "да"],
              })

              feature_cols = ["days_since_login", "tariff", "support_tickets"]
              target_col = "churn_after_30d"

              X = df.loc[:, feature_cols]
              y = df.loc[:, target_col]

              print(X.to_string(index=False))
              print(y.tolist())
              print("X:", X.shape, "y:", y.shape)
            `, `
               days_since_login tariff  support_tickets
                              2     pro                0
                             18   basic                2
                              5     pro                1
                             31   basic                4
                              9   basic                0
                             24     pro                3
              ['нет', 'да', 'нет', 'да', 'нет', 'да']
              X: (6, 3) y: (6,)
            `, 'Число строк совпадает, а `churn_after_30d`, `client_id` и `household_group` отсутствуют в X.'),
          ],
          callouts: [
            callout('Соответствие строк', 'Шесть строк X ↔ шесть значений y. Потеря или перестановка строк разрушит соответствие объекта и ответа.', 'schema'),
          ],
        }),
        section('feature-check', 'Проверить каждый кандидат в признаки', [
          'Для каждого столбца задайте один и тот же вопрос: **«Был ли он доступен, одинаково измерен и допустим в момент реального `predict` (получения прогноза)?»** Все три условия обязательны.',
        ], {
          table: {
            headers: ['Кандидат', 'Доступен к cutoff?', 'Одинаково измерен?', 'Допустим?', 'Решение'],
            rows: [
              ['`days_since_login`', 'да, считаем на дату решения', 'да, одна формула давности', 'да', 'оставить'],
              ['`tariff`', 'да, берём тариф на дату решения', 'да, один справочник тарифов', 'да', 'оставить'],
              ['`support_tickets`', 'да, считаем только обращения до даты', 'да, единые правила учёта', 'да', 'оставить'],
            ],
          },
        }),
        section('categorical-values', 'Категории пока остаются исходными значениями', [
          '`tariff` - категориальный признак: в X пока остаются строки `basic` и `pro`. X на этом этапе означает таблицу входных признаков, а не обязательно уже готовую числовую матрицу.',
          'Преобразование категорий в числа будет разобрано позже, в теме 4.16. Здесь важно правильно выбрать строки, роли столбцов и момент доступности, не забегая в кодирование.',
        ]),
      ],
    ),
    theoryStep(
      'ml-foundations-data-target-leakage',
      'Момент прогноза и утечка данных',
      'Cutoff отделяет сведения, которыми можно располагать при решении, от ответа и другой информации из будущего.',
      [
        section('cutoff-timeline', 'До и после момента решения', [
          '**Момент прогноза (cutoff)** - граница времени, в которую система должна выдать прогноз. Для синтетической задачи оттока к cutoff известны `days_since_login`, `tariff` и число обращений, открытых до этой даты.',
          'Ответ `churn_after_30d` возникает только после 30-дневного окна. Дата закрытия аккаунта и обращения, созданные после cutoff, тоже относятся к будущему. Они могут храниться в исторической таблице, но не могут входить в X для такого решения.',
        ], {
          callouts: [
            callout('Временная граница', 'Доступно до cutoff → `predict` → исход после cutoff. Наличие столбца в архиве не доказывает, что он был доступен в момент решения.', 'schema'),
          ],
        }),
        section('leakage-types', 'Четыре разных источника утечки', [
          '**Утечка данных (data leakage)** возникает, когда построение или проверка модели использует информацию, которой не будет в честном реальном сценарии. Источник запрета нужно уметь назвать.',
        ], {
          table: {
            headers: ['Вид утечки', 'Короткий пример', 'Запрещённый источник информации'],
            rows: [
              ['утечка цели (target leakage)', '`churn_after_30d` включён в X', 'сам правильный ответ'],
              ['утечка из будущего (future leakage)', '`account_closed_at` известно только после cutoff', 'будущее относительно решения'],
              ['утечка подготовки (preprocessing leakage)', 'медиану рассчитали сразу по train и test', 'статистика отложенных данных'],
              ['утечка групп/дубликатов (group/duplicate leakage)', 'строки связанной группы H1 попали и в train, и в test', 'почти тот же связанный объект из проверки'],
            ],
          },
          callouts: [
            callout('Диагностика', 'Спросите, откуда взялось значение и было ли оно доступно ровно тем же способом при реальном прогнозе.', 'remember'),
          ],
        }),
      ],
    ),
    quizStep(
      'ml-foundations-data-target-quiz',
      'Определить target синтетической таблицы',
      'Проверяем роль ответа и момент его появления.',
      singleQuiz(
        'quiz-ml-foundations-data-target',
        'Features и target',
        'ml-foundations-data-target',
        'ml-foundations',
        'Для клиента C106 нужно предсказать отток в следующие 30 дней. Какой столбец должен быть y?',
        [
          { id: 'a', text: '`days_since_login`' },
          { id: 'b', text: '`tariff`' },
          { id: 'c', text: '`support_tickets`' },
          { id: 'd', text: '`churn_after_30d`' },
        ],
        'd',
        '`y` хранит целевой ответ. Для C106 признаки известны к cutoff, а `churn_after_30d` становится известен после 30-дневного окна.',
      ),
    ),
    practiceStep(
      'ml-foundations-data-target-practice',
      'Отделить predictors от target, id и group',
      'По ролям колонок выводим только допустимые признаки.',
      makeStdinTask(
        'task-ml-foundations-split-x-y',
        'Разделить колонки по ролям',
        'Первая строка содержит названия колонок, вторая - target. Необязательные третья и четвёртая строки содержат id- и group-колонки. Выведите только predictors (предикторы) в исходном порядке.',
        `
          import sys

          lines = sys.stdin.read().splitlines()
          # TODO: сформируйте множество target, id и group.

          # TODO: исключите эти роли из columns и выведите predictors.
        `,
        [
          { id: 's1', description: 'Квартиры (сохранённый сценарий)', input: 'area rooms district price\nprice', expectedOutput: 'area rooms district' },
          { id: 's2', description: 'Синтетические клиенты', input: 'client_id household_group days_since_login tariff support_tickets churn_after_30d\nchurn_after_30d\nclient_id\nhousehold_group', expectedOutput: 'days_since_login tariff support_tickets' },
        ],
        [
          { id: 'h1', description: 'Клиенты (сохранённый сценарий)', input: 'age purchases churn\nchurn', expectedOutput: 'age purchases' },
          { id: 'h2', description: 'Target в середине (сохранённый сценарий)', input: 'text is_spam sender\nis_spam', expectedOutput: 'text sender' },
          { id: 'h3', description: 'Несколько идентификаторов', input: 'row_id patient_id clinic age marker outcome\noutcome\nrow_id patient_id\nclinic', expectedOutput: 'age marker' },
        ],
        `
          import sys

          lines = sys.stdin.read().splitlines()
          columns = lines[0].split()
          excluded = {lines[1].strip()}
          if len(lines) > 2:
              excluded.update(lines[2].split())
          if len(lines) > 3:
              excluded.update(lines[3].split())
          predictors = [column for column in columns if column not in excluded]
          print(" ".join(predictors))
        `,
      ),
    ),
  ],
)
