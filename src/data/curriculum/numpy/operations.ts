import { callout, code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicNumpyVectorOps = numpyTopic(
  'numpy-vector-operations',
  '2.5 Векторные операции',
  5,
  'Показываем, как применять операции ко всему признаку или массиву без ручного цикла.',
  'Векторные операции позволяют считать над всеми значениями сразу: масштабировать признаки, считать ошибки и нормализовать данные.',
  ['vectorization', 'np.abs', 'errors', 'normalization', 'elementwise operations'],
  ['errors = y_pred - y_true', 'absolute_errors = np.abs(errors)'],
  [
    'Арифметика NumPy применяется поэлементно.',
    '`np.abs()` полезна для модулей ошибок.',
    'Векторизация делает код ближе к формуле анализа данных.',
  ],
  [
    theoryStep(
      'numpy-vector-scenario',
      'Операция сразу над признаком',
      'Разбираем рабочие сценарии векторизации.',
      [
        section('why', 'В ML редко считают по одному элементу', [
          'Если нужно прибавить константу ко всем значениям, умножить признак или посчитать ошибку предсказаний, цикл обычно только прячет смысл. В NumPy действие записывается как операция над массивом.',
          'Это читается ближе к задаче: `prices * 0.9` - снизить все цены на 10%, `y_pred - y_true` - получить ошибки модели.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              y_true = np.array([100, 200, 300])
              y_pred = np.array([110, 190, 330])

              errors = y_pred - y_true
              absolute_errors = np.abs(errors)

              print(errors)
              print(absolute_errors)
            `, `
              [ 10 -10  30]
              [10 10 30]
            `, 'Разность дала знаковые ошибки, а `np.abs()` превратил их в абсолютные ошибки.'),
          ],
        }),
        section('normalization', 'Нормализация как сценарий', [
          'Перед обучением модели признаки часто приводят к сопоставимому масштабу. Например, можно вычесть минимум и разделить на диапазон значений.',
          'Важно увидеть общую форму: сначала считаем параметры признака, затем применяем их ко всему массиву.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              area = np.array([35, 52, 80])
              scaled = (area - area.min()) / (area.max() - area.min())

              print(scaled)
            `, '[0.         0.37777778 1.        ]', 'Одна формула сработала для всех значений признака.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-vector-checkpoint',
      'Что теперь умеем',
      'Закрепляем смысл векторных операций.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Векторизация полезна, когда действие одинаковое для всех элементов массива.',
        ], {
          bullets: [
            'прибавлять и умножать константу для всего признака;',
            'считать разность между предсказанием и target;',
            'получать абсолютные ошибки через `np.abs()`;',
            'записывать простую нормализацию одной формулой.',
          ],
          callouts: [
            callout('Типичная ошибка', 'Смешивать массивы разной длины. Для поэлементной операции `y_pred` и `y_true` должны соответствовать одним и тем же объектам.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-vector-loop-comparison',
      'Почему не цикл',
      'Сравниваем смысл векторной записи и ручного обхода.',
      [
        section('loop', 'Векторизация показывает намерение', [
          'Цикл по элементам иногда нужен, но для одинаковой арифметики над массивом он часто делает код длиннее и менее понятным. Запись `np.abs(y_pred - y_true).mean()` сразу показывает аналитический смысл: взять ошибки, убрать знак и усреднить.',
          'Векторная запись также снижает риск ошибиться с индексом. Если `y_pred` и `y_true` относятся к одним и тем же объектам, поэлементная операция сохраняет соответствие между предсказанием и правильным ответом.',
        ], {
          callouts: [
            callout('Что теперь умеем', 'Записывать простые вычисления над признаками, target и ошибками как операции над массивами, а не как ручной цикл.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-vector-operations-quiz-error',
      'Ошибка модели',
      'Проверяем выбор операции для ошибок.',
      singleQuiz(
        'quiz-numpy-vector-operations-error',
        'Векторные ошибки',
        'numpy-vector-operations',
        'numpy-ml',
        'Как получить массив ошибок предсказаний, если есть `y_pred` и `y_true`?',
        [
          { id: 'a', text: '`y_pred - y_true`' },
          { id: 'b', text: '`y_pred.shape`' },
          { id: 'c', text: '`np.array(y_pred, y_true)`' },
          { id: 'd', text: '`y_true.dtype`' },
        ],
        'a',
        'Разность двух массивов одинаковой длины считается поэлементно.',
      ),
    ),
    quizStep(
      'numpy-vector-operations-quiz-tool',
      'Абсолютная ошибка',
      'Выбираем функцию по задаче.',
      singleQuiz(
        'quiz-numpy-vector-operations-tool',
        'Абсолютные значения',
        'numpy-vector-operations',
        'numpy-ml',
        'Какая функция убирает знак у ошибок и оставляет величину отклонения?',
        [
          { id: 'a', text: '`np.abs()`' },
          { id: 'b', text: '`np.mean()`' },
          { id: 'c', text: '`np.arange()`' },
          { id: 'd', text: '`np.where()`' },
        ],
        'a',
        '`np.abs()` возвращает модуль каждого элемента.',
      ),
    ),
    practiceStep(
      'numpy-vector-operations-practice',
      'Средняя абсолютная ошибка',
      'Считаем MAE через векторные операции.',
      makeStdinTask(
        'task-numpy-mae',
        'Средняя абсолютная ошибка',
        'На первой строке даны истинные значения, на второй - предсказания. Посчитайте среднюю абсолютную ошибку и выведите число.',
        `
          import numpy as np

          # TODO: считайте истинные значения

          # TODO: считайте предсказания

          # TODO: посчитайте абсолютные ошибки

          # TODO: выведите среднюю ошибку
        `,
        [
          { id: 's1', description: 'Три предсказания', input: '100 200 300\n110 190 330', expectedOutput: '16.666666666666668' },
        ],
        [
          { id: 'h1', description: 'Без ошибки', input: '1 2 3\n1 2 3', expectedOutput: '0.0' },
        ],
        `
          import numpy as np

          y_true = np.array(list(map(float, input().split())))
          y_pred = np.array(list(map(float, input().split())))
          errors = np.abs(y_pred - y_true)
          print(errors.mean())
        `,
      ),
    ),
  ],
)

export const topicNumpyBroadcasting = numpyTopic(
  'numpy-broadcasting',
  '2.9 Broadcasting',
  9,
  'Объясняем, как NumPy выполняет операции между массивами разных, но совместимых форм.',
  'Broadcasting позволяет применить один вектор параметров ко всей матрице: например, вычесть среднее каждого признака из всех строк.',
  ['broadcasting', 'compatible shapes', 'axis', 'centering'],
  ['формы совместимы, если размеры осей равны или один из них равен 1'],
  [
    'Broadcasting не копирует данные вручную, а логически растягивает меньший массив.',
    'Частый ML-сценарий: центрировать признаки через `X - X.mean(axis=0)`.',
    'Если формы несовместимы, NumPy не угадает намерение.',
  ],
  [
    theoryStep(
      'numpy-broadcasting-scenario',
      'Центрирование признаков',
      'Показываем broadcasting на типичном ML-примере.',
      [
        section('centering', 'Один вектор для всей матрицы', [
          'Допустим, `X` хранит объекты и признаки. Чтобы центрировать данные, нужно посчитать среднее каждого столбца и вычесть этот вектор из каждой строки матрицы.',
          'Формы выглядят так: `X.shape = (3, 2)`, `mean.shape = (2,)`, результат `X - mean` снова имеет форму `(3, 2)`.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[10, 100],
                            [20, 200],
                            [30, 300]])

              mean = X.mean(axis=0)
              centered = X - mean

              print(mean)
              print(centered)
            `, `
              [ 20. 200.]
              [[ -10. -100.]
               [   0.    0.]
               [  10.  100.]]
            `, 'Вектор средних вычелся из каждой строки матрицы.'),
          ],
          callouts: [
            callout('Схема', 'X.shape = (3, 2)\nmean.shape = (2,)\nX - mean -> (3, 2)', 'schema'),
          ],
        }),
        section('rules', 'Когда формы совместимы', [
          'Broadcasting работает, если размеры осей равны или один из них равен `1`. Проверка идёт справа налево по формам.',
          'Это удобно, но не магия: NumPy не знает смысл ваших признаков. Он проверяет только совместимость размеров.',
        ], {
          callouts: [
            callout('Типичная ошибка', 'Если попробовать вычесть вектор формы `(3,)` из матрицы `(3, 2)`, NumPy не поймёт, применять его к строкам или столбцам, и выдаст ошибку совместимости.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-checkpoint',
      'Что теперь умеем',
      'Закрепляем, когда применять broadcasting.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Broadcasting полезен, когда нужно применить маленький набор параметров ко всей матрице. В подготовке данных это часто означает: один вектор средних, минимумов, максимумов или стандартных отклонений применяется ко всем строкам `X`.',
          'Главная проверка перед такой операцией - формы массивов. Если параметры посчитаны по столбцам, их форма должна соответствовать числу признаков. Тогда NumPy применит их к каждой строке матрицы и сохранит структуру объектов.',
        ], {
          bullets: [
            'вычитать среднее каждого признака из всей матрицы;',
            'делить признаки на стандартное отклонение;',
            'читать формы перед операцией;',
            'объяснять ошибку несовместимых форм.',
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-normalization',
      'Нормализация через broadcasting',
      'Показываем полный сценарий масштабирования признаков.',
      [
        section('normalize', 'Один набор параметров на все строки', [
          'При min-max нормализации для каждого признака считают минимум и максимум, а затем применяют формулу ко всей матрице. Вектор минимумов имеет длину по числу столбцов, и broadcasting применяет его к каждой строке.',
          'Такой подход сохраняет смысл данных: каждый столбец масштабируется своими параметрами, а каждая строка остаётся тем же объектом, только в новом числовом масштабе.',
        ], {
          callouts: [
            callout('Сценарий', '1. `mins = X.min(axis=0)`\n2. `maxs = X.max(axis=0)`\n3. `(X - mins) / (maxs - mins)`', 'schema'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-shape-reading',
      'Как читать формы справа налево',
      'Формулируем практическое правило совместимости.',
      [
        section('reading', 'Совпадают последние оси', [
          'Для матрицы `(3, 2)` и вектора `(2,)` сравнение начинается справа: `2` совпадает с `2`, значит операция возможна. Для матрицы `(3, 2)` и вектора `(3,)` последние размеры `2` и `3` не совпадают, поэтому такая операция не подходит.',
          'Это правило помогает понять сообщение об ошибке broadcasting. NumPy не знает, что вектор длины 3 мог означать строки; он видит только несовместимые размеры последних осей.',
        ], {
          callouts: [
            callout('Типичная ошибка', 'Считать параметры по строкам, а потом пытаться применить их как параметры по столбцам. Перед операцией всегда смотрите `X.shape` и форму вектора параметров.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-recap',
      'Когда broadcasting уместен',
      'Отделяем удобный сценарий от ошибки формы.',
      [
        section('recap', 'Применяем вектор параметров к матрице', [
          'Broadcasting особенно хорош, когда маленький массив действительно описывает параметры большого массива: средние по признакам, стандартные отклонения, минимумы, максимумы, веса признаков.',
          'Если меньший массив не имеет такого смысла, лучше остановиться и явно проверить форму. Broadcasting делает код короче, но ответственность за смысл операции остаётся на разработчике.',
        ], {
          callouts: [
            callout('Промежуточный вывод', 'Broadcasting полезен не потому, что “растягивает всё подряд”, а потому что позволяет аккуратно применить осмысленный вектор параметров ко всей матрице.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-broadcasting-quiz-compatible',
      'Совместимость форм',
      'Проверяем правило broadcasting.',
      singleQuiz(
        'quiz-numpy-broadcasting-compatible',
        'Broadcasting',
        'numpy-broadcasting',
        'numpy-ml',
        'Можно ли вычесть массив формы `(2,)` из матрицы формы `(3, 2)`?',
        [
          { id: 'a', text: 'Да, вектор применится к каждому ряду матрицы' },
          { id: 'b', text: 'Нет, формы всегда должны быть полностью одинаковыми' },
          { id: 'c', text: 'Да, но результат станет одномерным' },
          { id: 'd', text: 'Нет, broadcasting работает только со строками' },
        ],
        'a',
        'Последняя ось совпадает: `2` и `2`, поэтому вектор можно применить к каждой строке.',
      ),
    ),
    quizStep(
      'numpy-broadcasting-quiz-use',
      'ML-сценарий',
      'Выбираем, где broadcasting особенно полезен.',
      singleQuiz(
        'quiz-numpy-broadcasting-use',
        'Broadcasting в ML',
        'numpy-broadcasting',
        'numpy-ml',
        'Какой пример лучше всего показывает broadcasting в подготовке признаков?',
        [
          { id: 'a', text: 'Вычесть среднее каждого столбца из всей матрицы `X`' },
          { id: 'b', text: 'Открыть CSV-файл через pandas' },
          { id: 'c', text: 'Переименовать столбец таблицы' },
          { id: 'd', text: 'Вывести тип Python-списка' },
        ],
        'a',
        'Один вектор средних применяется ко всем строкам матрицы признаков.',
      ),
    ),
    practiceStep(
      'numpy-broadcasting-practice',
      'Центрировать матрицу',
      'Вычитаем среднее каждого столбца из матрицы.',
      makeStdinTask(
        'task-numpy-center-matrix',
        'Центрировать матрицу',
        'На вход подаются `n`, `m`, затем `n * m` чисел. Создайте матрицу и выведите матрицу после вычитания среднего каждого столбца.',
        `
          import numpy as np

          # TODO: считайте размеры и значения

          # TODO: создайте матрицу

          # TODO: посчитайте среднее по столбцам

          # TODO: выведите центрированную матрицу
        `,
        [
          { id: 's1', description: 'Три строки и два столбца', input: '3 2\n10 100 20 200 30 300', expectedOutput: '[[ -10. -100.]\n [   0.    0.]\n [  10.  100.]]' },
        ],
        [
          { id: 'h1', description: 'Два объекта', input: '2 2\n1 3 5 7', expectedOutput: '[[-2. -2.]\n [ 2.  2.]]' },
        ],
        `
          import numpy as np

          n, m = map(int, input().split())
          values = list(map(float, input().split()))
          X = np.array(values).reshape(n, m)
          centered = X - X.mean(axis=0)
          print(centered)
        `,
      ),
    ),
  ],
)
