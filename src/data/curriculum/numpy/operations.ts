import { callout, code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'



export const topicNumpyVectorOps = numpyTopic(
  'numpy-vector-operations',
  '2.5 Векторные операции',
  5,
  'Разбираем операции с числом, операции массивов и векторизацию вместо цикла.',
  'NumPy применяет арифметику ко всем элементам массива сразу.',
  ['vectorization', 'elementwise operation', 'array arithmetic'],
  ['x + 10', 'x * y'],
  ['Векторизация делает код короче и ближе к математической записи.'],
  [
    theoryStep(
      'numpy-vector-operations-scalar',
      'Операции с числом',
      'Число применяется к каждому элементу массива.',
      [
        section('scalar', 'Один оператор - весь массив', [
          '**NumPy применяет операцию ко всем элементам сразу**. Если прибавить число к массиву, число прибавится к каждому элементу. То же работает для `-`, `*`, `/`, `**`.',
          'Это удобно для масштабирования признаков, перевода единиц измерения, добавления смещения и быстрых учебных вычислений.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])

              print("x + 10:", x + 10)
              print("x ** 2:", x ** 2)
            `, 'x + 10: [11 12 13]\nx ** 2: [1 4 9]', 'Каждая операция применена к каждому элементу массива.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-vector-operations-arrays',
      'Операции с массивами',
      'Массивы одинаковой формы складываются поэлементно.',
      [
        section('arrays', 'Поэлементная арифметика', [
          'Если формы массивов совпадают, операции выполняются поэлементно: первый с первым, второй со вторым, третий с третьим. Так можно быстро считать ошибки, разности признаков и новые числовые признаки.',
          'Если формы не совпадают, NumPy либо применит broadcasting, либо выдаст ошибку. Broadcasting отдельно разбирается дальше.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              a = np.array([10, 20, 30])
              b = np.array([1, 2, 3])

              print("a - b:", a - b)
              print("a / b:", a / b)
            `, 'a - b: [ 9 18 27]\na / b: [10. 10. 10.]', 'Результаты показывают поэлементную разность и деление.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-vector-operations-vectorization',
      'Векторизация вместо цикла',
      'Векторизация заменяет ручной цикл операцией над массивом.',
      [
        section('list-vs-array', 'Почему не цикл', [
          'В обычном Python для обработки каждого элемента часто пишут цикл. В NumPy во многих задачах лучше записать операцию над массивом целиком: это короче, легче читается и лучше совпадает с математической формулой.',
          'Главная мысль не в том, что циклы запрещены. Важно сначала проверить, можно ли выразить задачу как операцию над массивом целиком.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              prices = np.array([100, 200, 300])
              discounted = prices * 0.9

              print(discounted)
            `, '[ 90. 180. 270.]', 'Скидка 10% применена сразу ко всем ценам без цикла.'),
          ],
          callouts: [
            callout('Вывод', 'Векторизация делает действие над всеми элементами явным: `prices * 0.9` читается как “умножь все цены на 0.9”.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-vector-operations-quiz-result',
      'Результат операции',
      'Проверяем умножение массива на число.',
      singleQuiz(
        'quiz-numpy-vector-operations-result',
        'Операция с числом',
        'numpy-vector-operations',
        'numpy-ml',
        'Что получится у `np.array([1, 2, 3]) * 2`?',
        [
          { id: 'a', text: '`[1, 2, 3, 1, 2, 3]`' },
          { id: 'b', text: '`[2, 4, 6]`' },
          { id: 'c', text: '`[1, 2, 3, 2]`' },
          { id: 'd', text: '`2`' },
        ],
        'b',
        'NumPy умножает каждый элемент массива на 2.',
      ),
    ),
    quizStep(
      'numpy-vector-operations-quiz-loop',
      'Почему не цикл',
      'Проверяем идею векторизации.',
      singleQuiz(
        'quiz-numpy-vector-operations-loop',
        'Векторизация',
        'numpy-vector-operations',
        'numpy-ml',
        'Почему в NumPy часто избегают ручных циклов?',
        [
          { id: 'a', text: 'Векторные операции короче и применяются к массиву целиком' },
          { id: 'b', text: 'Циклы не существуют в Python' },
          { id: 'c', text: 'NumPy работает только со строками' },
          { id: 'd', text: 'Массивы нельзя умножать' },
        ],
        'a',
        'Векторные операции выражают действие над массивом целиком и обычно читаются проще.',
      ),
    ),
    practiceStep(
      'numpy-vector-operations-practice',
      'Выполнить операции',
      'Практика на векторные операции.',
      makeStdinTask(
        'task-numpy-vector-ops',
        'Выполнить операции',
        'На вход подаются числа. Создайте массив, выведите массив, умноженный на 2, и массив, возведённый в квадрат.',
        `
          import numpy as np

          # TODO: считайте числа

          # TODO: создайте массив

          # TODO: выведите массив * 2

          # TODO: выведите массив ** 2
        `,
        [
          { id: 's1', description: 'Три числа', input: '1 2 3', expectedOutput: '[2 4 6]\n[1 4 9]' },
        ],
        [
          { id: 'h1', description: 'Другие числа', input: '2 5 10', expectedOutput: '[ 4 10 20]\n[  4  25 100]' },
          { id: 'h2', description: 'Нули', input: '0 1 0', expectedOutput: '[0 2 0]\n[0 1 0]' },
        ],
        `
          import numpy as np

          x = np.array(list(map(int, input().split())))
          print(x * 2)
          print(x ** 2)
        `,
      ),
    ),
  ],
)

export const topicNumpyBroadcasting = numpyTopic(
  'numpy-broadcasting',
  '2.9 Broadcasting',
  9,
  'Разбираем broadcasting на числе, правилах совместимости форм, векторе и центрировании признаков.',
  'Broadcasting позволяет выполнять операции с массивами разных, но совместимых форм.',
  ['broadcasting', 'scalar', 'vector', 'compatible shapes', 'centered features'],
  ['(3, 2) + (2,) → (3, 2)', 'X_centered = X - X.mean(axis=0)'],
  ['Broadcasting особенно полезен при нормализации и центрировании признаков.'],
  [
    theoryStep(
      'numpy-broadcasting-definition',
      'Что такое broadcasting',
      'Broadcasting расширяет меньший массив до совместимой формы логически, без лишних копий.',
      [
        section('definition', 'Идея broadcasting', [
          'Broadcasting - правило NumPy, которое позволяет выполнять операции с массивами разных, но совместимых форм. Меньший массив как будто растягивается до формы большего, но физически NumPy не создаёт лишние копии.',
          'Это делает код для нормализации, центрирования и сдвига признаков компактным: можно вычесть вектор средних из всей матрицы признаков одной строкой.',
        ], {
          callouts: [
            callout('Схема', 'маленькая форма → логически повторяется → операция с большой формой', 'schema'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-rules',
      'Правила совместимости форм',
      'Формы совместимы, если размеры осей равны или один из них равен 1.',
      [
        section('rules', 'Как проверить формы', [
          'NumPy сравнивает формы справа налево. Две оси совместимы, если их размеры равны или один из размеров равен 1. Если это верно для всех сравниваемых осей, операция выполняется.',
          'Например, `(3, 2) + (2,) -> (3, 2)`, потому что последняя ось равна 2. А `(3, 2) + (3,)` не сработает: последние размеры 2 и 3 не равны и ни один не равен 1.',
        ], {
          table: {
            headers: ['Операция', 'Можно?', 'Форма результата'],
            rows: [
              ['`(3, 2) + (2,)`', 'да', '`(3, 2)`'],
              ['`(3, 2) + (1, 2)`', 'да', '`(3, 2)`'],
              ['`(3, 2) + (3,)`', 'нет', 'ошибка формы'],
            ],
          },
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-scalar',
      'Скаляр и массив',
      'Число применяется к каждому элементу массива.',
      [
        section('scalar', 'Самый простой broadcasting', [
          'Самый простой пример broadcasting - операция массива с числом. Число ведёт себя так, как будто оно повторено до формы массива.',
          'Это используется постоянно: умножить все признаки на коэффициент, прибавить смещение, перевести единицы измерения или нормализовать значения.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([1, 2, 3])

              print("shape:", x.shape)
              print("x + 10:", x + 10)
            `, 'shape: (3,)\nx + 10: [11 12 13]', 'Скаляр 10 применился к каждому элементу массива формы `(3,)`.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-vector',
      'Матрица и вектор',
      'Вектор может примениться к каждой строке матрицы.',
      [
        section('matrix-vector', 'Форма `(3, 2) + (2,)`', [
          'Если матрица имеет форму `(3, 2)`, а вектор форму `(2,)`, NumPy применит вектор к каждой строке матрицы. Это работает, потому что последняя размерность совпадает.',
          'Так удобно прибавлять смещение к каждому признаку или вычитать средние значения признаков из матрицы.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2],
                            [3, 4],
                            [5, 6]])
              v = np.array([10, 20])

              print("X shape:", X.shape)
              print("v shape:", v.shape)
              print(X + v)
            `, 'X shape: (3, 2)\nv shape: (2,)\n[[11 22]\n [13 24]\n [15 26]]', 'Вектор `[10, 20]` добавился к каждой строке матрицы.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-broadcasting-centering',
      'Центрирование признаков',
      'Broadcasting часто используют для нормализации признаков.',
      [
        section('centering', 'Вычесть среднее каждого признака', [
          '`mean = X.mean(axis=0)` содержит среднее по каждому столбцу-признаку. Когда мы пишем `X - mean`, NumPy вычитает этот вектор из каждой строки матрицы.',
          'После центрирования среднее каждого столбца становится близким к нулю. Это полезно для анализа признаков и многих алгоритмов, чувствительных к масштабу данных.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[1, 2],
                            [3, 4],
                            [5, 6]])

              mean = X.mean(axis=0)
              X_centered = X - mean

              print("mean:", mean)
              print(X_centered)
              print("new mean:", X_centered.mean(axis=0))
            `, 'mean: [3. 4.]\n[[-2. -2.]\n [ 0.  0.]\n [ 2.  2.]]\nnew mean: [0. 0.]', 'Вывод показывает и исходные средние, и то, что после центрирования среднее стало нулевым.'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-broadcasting-quiz-compatible',
      'Можно ли выполнить операцию',
      'Проверяем совместимость `(3, 2)` и `(2,)`.',
      singleQuiz(
        'quiz-numpy-broadcasting-compatible',
        'Совместимость форм',
        'numpy-broadcasting',
        'numpy-ml',
        'Можно ли сложить массив формы `(3, 2)` и вектор формы `(2,)`?',
        [
          { id: 'a', text: 'Да, вектор применится к каждой строке' },
          { id: 'b', text: 'Нет, формы всегда должны быть полностью одинаковыми' },
          { id: 'c', text: 'Да, но только если все значения нулевые' },
          { id: 'd', text: 'Нет, NumPy не поддерживает сложение массивов' },
        ],
        'a',
        'Вектор формы `(2,)` совместим с последней размерностью матрицы `(3, 2)`, поэтому применится к каждой строке.',
      ),
    ),
    quizStep(
      'numpy-broadcasting-quiz-shape',
      'Форма результата',
      'Проверяем форму после broadcasting.',
      singleQuiz(
        'quiz-numpy-broadcasting-shape',
        'Форма результата',
        'numpy-broadcasting',
        'numpy-ml',
        'Какая форма будет у результата `X + v`, если `X.shape == (3, 2)`, а `v.shape == (2,)`?',
        [
          { id: 'a', text: '`(2,)`' },
          { id: 'b', text: '`(3,)`' },
          { id: 'c', text: '`(3, 2)`' },
          { id: 'd', text: '`(2, 3)`' },
        ],
        'c',
        'Результат операции с матрицей сохраняет форму матрицы `(3, 2)`.',
      ),
    ),
    practiceStep(
      'numpy-broadcasting-practice',
      'Центрировать матрицу',
      'Практика на центрирование признаков.',
      makeStdinTask(
        'task-numpy-center-matrix',
        'Центрировать матрицу',
        'На вход подаются 6 чисел. Создайте матрицу формы `(3, 2)`. Вычтите из каждого столбца его среднее. Выведите результат.',
        `
          import numpy as np

          # TODO: считайте 6 чисел

          # TODO: создайте матрицу формы (3, 2)

          # TODO: найдите среднее по столбцам

          # TODO: вычтите среднее из матрицы

          # TODO: выведите результат
        `,
        [
          { id: 's1', description: 'Последовательность 1..6', input: '1 2 3 4 5 6', expectedOutput: '[[-2. -2.]\n [ 0.  0.]\n [ 2.  2.]]' },
        ],
        [
          { id: 'h1', description: 'Сдвинутая последовательность', input: '2 4 4 6 6 8', expectedOutput: '[[-2. -2.]\n [ 0.  0.]\n [ 2.  2.]]' },
          { id: 'h2', description: 'Одинаковые строки', input: '1 1 1 1 1 1', expectedOutput: '[[0. 0.]\n [0. 0.]\n [0. 0.]]' },
        ],
        `
          import numpy as np

          values = list(map(float, input().split()))
          X = np.array(values).reshape(3, 2)
          mean = X.mean(axis=0)
          centered = X - mean
          print(centered)
        `,
      ),
    ),
  ],
)

