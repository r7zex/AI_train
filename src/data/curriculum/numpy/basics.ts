import { callout, code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicNumpyWhy = numpyTopic(
  'numpy-why',
  '2.1 Зачем нужен NumPy',
  1,
  'Показываем, почему ML почти всегда начинается с числовых массивов и чем ndarray отличается от списка Python.',
  'NumPy нужен не для массивов вообще, а для удобной и быстрой работы с числовыми данными: признаками, target, весами модели и ошибками.',
  ['NumPy', 'ndarray', 'array', 'vectorization', 'features', 'target'],
  ['таблица объектов -> числовая матрица признаков -> операции над признаками -> модель'],
  [
    'Строка матрицы обычно означает объект, столбец - признак.',
    '`ndarray` хранит числовые данные компактнее и применяет операции сразу ко всем элементам.',
    'NumPy лежит под pandas, sklearn и многими ML-библиотеками.',
  ],
  [
    theoryStep(
      'numpy-why-scenario',
      'Данные в ML становятся числами',
      'Связываем NumPy с обычным ML-пайплайном.',
      [
        section('pipeline', 'От таблицы к модели', [
          'В реальном ML данные редко остаются в виде “красивой таблицы”. Перед обучением модель получает **числовую матрицу признаков** `X` и часто отдельный вектор ответов `y`.',
          'Например, строка может описывать квартиру, а столбцы - площадь, число комнат и расстояние до метро. NumPy помогает хранить такие значения и выполнять над ними вычисления без ручного цикла по каждой ячейке.',
          'Типовая цепочка выглядит так: таблица объектов -> числовая матрица признаков -> операции над признаками -> модель.',
        ], {
          callouts: [
            callout('Где используется', 'Признаки модели, target, веса модели, батчи данных, нормализация, вычисление ошибок и генерация синтетических примеров.', 'example'),
            callout('Схема', 'X: строки = объекты, столбцы = признаки\ny: один ответ для каждого объекта', 'schema'),
          ],
        }),
        section('array-vs-list', '`ndarray` не просто список', [
          'Python-список удобен для хранения разных объектов. NumPy-массив удобен, когда данные числовые и над ними нужно считать: умножать, вычитать, усреднять, фильтровать.',
          'Главное отличие видно на простой операции: список с `* 2` повторяется, а NumPy-массив умножает каждый элемент.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              values_list = [1, 2, 3]
              values_array = np.array([1, 2, 3])

              print(values_list * 2)
              print(values_array * 2)
            `, `
              [1, 2, 3, 1, 2, 3]
              [2 4 6]
            `, 'Один и тот же знак имеет разный смысл: список повторился, а NumPy применил умножение ко всем элементам.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-why-checkpoint',
      'Что теперь умеем',
      'Фиксируем смысл NumPy перед переходом к созданию массивов.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'После этой темы важно понимать не синтаксис одной функции, а роль NumPy в анализе данных.',
        ], {
          bullets: [
            'ML-модели обычно работают с числовыми признаками.',
            '`ndarray` хранит числовые данные и применяет операции ко всему массиву.',
            'Строка матрицы часто означает объект, столбец - признак.',
            'Векторизация делает код короче и обычно быстрее ручных циклов.',
          ],
          callouts: [
            callout('Типичная ошибка', 'Думать, что NumPy нужен только “для скорости”. В учебных задачах скорость может быть не видна, но форма данных, векторные операции и единый dtype понадобятся постоянно.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-why-where-used',
      'Где NumPy встретится дальше',
      'Показываем, что эта тема будет возвращаться в следующих разделах.',
      [
        section('usage', 'NumPy как общий язык чисел', [
          'Дальше в курсе NumPy будет появляться не отдельной справкой, а рабочим языком числовых операций. Когда мы считаем ошибку модели, нормализуем признак, строим маску выбросов или фиксируем случайный seed, мы снова используем одну и ту же идею: данные представлены массивами, а операции применяются к ним целиком.',
          'Поэтому сейчас достаточно понять базовую роль `ndarray`. Не нужно запоминать все функции заранее. Важнее видеть, какую проблему решает массив: хранит числовые признаки, сохраняет форму данных и позволяет записывать вычисления ближе к математической формуле.',
        ], {
          callouts: [
            callout('Промежуточный вывод', 'NumPy стоит воспринимать как основу числового этапа ML: матрицы признаков, векторы ответов, ошибки, нормализация и случайные эксперименты.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-why-quiz-scenario',
      'Где NumPy в ML',
      'Проверяем понимание роли массива в ML-сценарии.',
      singleQuiz(
        'quiz-numpy-why-scenario',
        'NumPy в ML',
        'numpy-why',
        'numpy-ml',
        'Что чаще всего означает строка в матрице признаков `X`?',
        [
          { id: 'a', text: 'Один объект наблюдения: квартира, клиент, заказ или измерение' },
          { id: 'b', text: 'Название одного признака' },
          { id: 'c', text: 'Среднее значение по всему датасету' },
          { id: 'd', text: 'Случайный seed эксперимента' },
        ],
        'a',
        'В типичной матрице `X` строки - это объекты, а столбцы - признаки этих объектов.',
      ),
    ),
    quizStep(
      'numpy-why-quiz-vectorization',
      'Зачем векторизация',
      'Выбираем правильное объяснение NumPy-операций.',
      singleQuiz(
        'quiz-numpy-why-vectorization',
        'Векторизация',
        'numpy-why',
        'numpy-ml',
        'Почему выражение `arr * 2` удобно для числовых данных?',
        [
          { id: 'a', text: 'Оно умножает каждый элемент массива без ручного цикла' },
          { id: 'b', text: 'Оно всегда обучает модель в два раза быстрее' },
          { id: 'c', text: 'Оно превращает числа в строки' },
          { id: 'd', text: 'Оно удаляет пропуски из таблицы' },
        ],
        'a',
        'NumPy применяет арифметические операции ко всем элементам массива сразу.',
      ),
    ),
    practiceStep(
      'numpy-why-practice',
      'Первый массив признаков',
      'Создаём NumPy-массив из входных чисел и выводим его.',
      makeStdinTask(
        'task-numpy-first-array',
        'Первый массив признаков',
        'На вход подаются числа одного признака. Считайте их, создайте NumPy-массив и выведите массив.',
        `
          import numpy as np

          # TODO: считайте числа из input()

          # TODO: создайте NumPy-массив

          # TODO: выведите массив
        `,
        [
          { id: 's1', description: 'Три значения признака', input: '1 2 3', expectedOutput: '[1 2 3]' },
        ],
        [
          { id: 'h1', description: 'Пять значений признака', input: '5 4 3 2 1', expectedOutput: '[5 4 3 2 1]' },
          { id: 'h2', description: 'Один объект', input: '10', expectedOutput: '[10]' },
        ],
        `
          import numpy as np

          values = list(map(int, input().split()))
          arr = np.array(values)
          print(arr)
        `,
      ),
    ),
  ],
)

export const topicNumpyShape = numpyTopic(
  'numpy-shape-ndim-dtype',
  '2.3 Форма, размерность и тип данных',
  3,
  'Разбираем `shape`, `ndim`, `size`, `dtype`, `astype()` и типичную ошибку с одномерным массивом.',
  'Форма массива показывает, как данные разложены по осям; в ML это критично, потому что модель ожидает конкретный вид `X` и `y`.',
  ['shape', 'ndim', 'size', 'dtype', 'astype', 'n_samples', 'n_features'],
  ['X.shape = (n_samples, n_features)', 'y.shape = (n_samples,)'],
  [
    '`shape` отвечает за форму данных.',
    '`ndim` показывает число измерений.',
    '`dtype` и `astype()` помогают контролировать типы чисел.',
  ],
  [
    theoryStep(
      'numpy-shape-ml',
      'Почему форма ломает ML-код',
      'Связываем форму массива с ожиданиями модели.',
      [
        section('shape-role', 'Матрица признаков и вектор target', [
          'Одна из самых частых ошибок новичка в ML - передать данные не той формы. Модель может ожидать двумерную матрицу `X`, а получить одномерный массив.',
          '`X.shape = (n_samples, n_features)` означает: сколько объектов и сколько признаков у каждого объекта. `y.shape = (n_samples,)` означает один ответ на каждый объект.',
        ], {
          callouts: [
            callout('Схема', 'X.shape = (100, 3) -> 100 объектов и 3 признака\ny.shape = (100,) -> 100 ответов', 'schema'),
            callout('Типичная ошибка', '`x.shape = (100,)` и `x.shape = (100, 1)` - не одно и то же. В первом случае это один вектор, во втором - таблица из 100 строк и одного столбца.', 'important'),
          ],
        }),
        section('attributes', 'Что смотреть первым', [
          '`shape`, `ndim`, `size` и `dtype` - это быстрый осмотр массива. Они не меняют данные, а помогают понять, что лежит в переменной.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[35, 1],
                            [52, 2],
                            [80, 3]])

              print(X.shape)
              print(X.ndim)
              print(X.size)
              print(X.dtype)
            `, `
              (3, 2)
              2
              6
              int32
            `, 'У массива 3 строки, 2 столбца, 2 измерения и 6 элементов. Тип может отличаться по платформе, но смысл проверки тот же.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-shape-dtype',
      'Тип данных и приведение',
      'Показываем, когда нужен `astype()`.',
      [
        section('dtype', '`dtype` отвечает за тип элементов', [
          'В одном NumPy-массиве обычно один общий тип элементов. Это помогает считать быстро, но требует внимания: целые числа, дробные числа и строки ведут себя по-разному.',
          '`astype()` используют, когда нужно привести данные к нужному типу: например, сделать значения дробными перед нормализацией.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              rooms = np.array([1, 2, 3])
              rooms_float = rooms.astype(float)

              print(rooms.dtype)
              print(rooms_float.dtype)
            `, `
              int32
              float64
            `, '`astype(float)` вернул новый массив с дробным типом. Исходный массив не обязан меняться.'),
          ],
          callouts: [
            callout('Что теперь умеем', '`shape` проверяет форму; `ndim` показывает число измерений; `size` считает элементы; `dtype` показывает тип; `astype()` приводит значения к новому типу.', 'summary'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-shape-common-check',
      'Мини-чек перед моделью',
      'Собираем быстрый осмотр массива в один рабочий шаг.',
      [
        section('check', 'Что проверить перед обучением', [
          'Перед передачей данных в модель полезно сделать маленькую проверку: посмотреть `X.shape`, `y.shape`, `X.ndim` и `X.dtype`. Этот шаг занимает несколько секунд, но часто сразу объясняет ошибку обучения.',
          'Если `X.ndim` равен `1`, перед нами вектор, а не матрица признаков. Если `dtype` строковый, значит в данных ещё есть категории или неправильно прочитанные числа. Если длина `y` не совпадает с числом строк `X`, объекты и ответы разъехались.',
        ], {
          callouts: [
            callout('Рабочая привычка', 'Когда модель ругается на форму входа, первым делом печатайте `X.shape` и `y.shape`, а не переписывайте алгоритм.', 'remember'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-shape-quiz-ml',
      'Форма для модели',
      'Проверяем отличие вектора от матрицы с одним признаком.',
      singleQuiz(
        'quiz-numpy-shape-ml',
        'Форма массива',
        'numpy-shape-ndim-dtype',
        'numpy-ml',
        'Модель ждёт матрицу из 100 объектов и 1 признака. Какая форма подходит?',
        [
          { id: 'a', text: '`(100, 1)`' },
          { id: 'b', text: '`(100,)`' },
          { id: 'c', text: '`(1,)`' },
          { id: 'd', text: '`(2, 50)`' },
        ],
        'a',
        '`(100, 1)` - двумерная матрица: 100 строк и 1 столбец признака.',
      ),
    ),
    quizStep(
      'numpy-shape-quiz-tool',
      'Выбрать инструмент',
      'Проверяем, какой атрибут нужен для задачи.',
      singleQuiz(
        'quiz-numpy-shape-tool',
        'Атрибуты массива',
        'numpy-shape-ndim-dtype',
        'numpy-ml',
        'Что лучше вызвать, чтобы быстро узнать тип элементов массива?',
        [
          { id: 'a', text: '`arr.dtype`' },
          { id: 'b', text: '`arr.shape`' },
          { id: 'c', text: '`arr.ndim`' },
          { id: 'd', text: '`arr.size`' },
        ],
        'a',
        '`dtype` показывает тип элементов: например, `int64`, `float64` или строковый тип.',
      ),
    ),
    practiceStep(
      'numpy-shape-practice',
      'Проверить форму матрицы',
      'Создаём матрицу и выводим её форму, размерность и число элементов.',
      makeStdinTask(
        'task-numpy-shape-check',
        'Проверить форму матрицы',
        'На вход подаются `n`, `m`, затем `n * m` чисел. Создайте матрицу размера `n x m` и выведите `shape`, `ndim`, `size` на отдельных строках.',
        `
          import numpy as np

          # TODO: считайте n и m

          # TODO: считайте значения

          # TODO: создайте массив и придайте ему форму n x m

          # TODO: выведите shape, ndim и size
        `,
        [
          { id: 's1', description: 'Две строки и три признака', input: '2 3\n1 2 3 4 5 6', expectedOutput: '(2, 3)\n2\n6' },
        ],
        [
          { id: 'h1', description: 'Один признак', input: '3 1\n10 20 30', expectedOutput: '(3, 1)\n2\n3' },
        ],
        `
          import numpy as np

          n, m = map(int, input().split())
          values = list(map(int, input().split()))
          arr = np.array(values).reshape(n, m)
          print(arr.shape)
          print(arr.ndim)
          print(arr.size)
        `,
      ),
    ),
  ],
)
