import { callout, code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicNumpyAggregations = numpyTopic(
  'numpy-aggregations-statistics',
  '2.6 Агрегации и статистики',
  6,
  'Смотрим распределение признака: минимум, максимум, среднее, медиану, разброс и квантили.',
  'Перед обучением модели нужно понять значения признака: где центр, какой разброс и есть ли подозрительные хвосты.',
  ['sum', 'mean', 'min', 'max', 'var', 'std', 'median', 'quantile', 'percentile'],
  ['mean чувствителен к выбросам', 'median устойчивее к выбросам'],
  [
    'Сначала смотрят минимум и максимум.',
    'Потом сравнивают `mean` и `median`.',
    '`std`, `quantile` и `percentile` помогают увидеть разброс и хвосты.',
  ],
  [
    theoryStep(
      'numpy-stats-scenario',
      'Первый взгляд на признак',
      'Группируем статистики как EDA-сценарий.',
      [
        section('eda', 'До модели нужно понять распределение', [
          'Перед обучением модели полезно понять, какие значения “типичны” для признака. Например, у `price` можно посчитать среднюю цену, но если в данных есть очень дорогой объект, среднее может сильно сместиться.',
          'Поэтому статистики лучше смотреть группами: базовые агрегаты, разброс и устойчивые меры.',
        ], {
          table: {
            headers: ['Группа', 'Функции', 'Зачем'],
            rows: [
              ['Базовые агрегаты', '`sum`, `mean`, `min`, `max`', 'быстро оценить масштаб и границы'],
              ['Разброс', '`var`, `std`', 'понять, насколько значения отличаются друг от друга'],
              ['Устойчивые статистики', '`median`, `quantile`, `percentile`', 'смотреть центр и хвосты при выбросах'],
            ],
          },
        }),
        section('outlier', 'Среднее и медиана при выбросе', [
          '`np.mean()` считает среднее значение, а `np.median()` показывает значение в середине отсортированного ряда. Медиана часто устойчивее к выбросам.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              x = np.array([10, 20, 30, 1000])

              print(np.mean(x))
              print(np.median(x))
            `, `
              265.0
              25.0
            `, 'Среднее сильно выросло из-за `1000`, а медиана осталась ближе к обычным значениям.'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-stats-checkpoint',
      'Что теперь умеем',
      'Фиксируем порядок анализа числового признака.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Статистики отвечают не на вопрос “какую функцию вспомнить”, а на вопрос “что происходит с признаком”.',
        ], {
          bullets: [
            'сначала смотрим минимум и максимум;',
            'потом сравниваем `mean` и `median`;',
            'разброс оцениваем через `std` или `var`;',
            'хвосты распределения смотрим через `quantile` или `percentile`;',
            'выбросы могут менять выводы по среднему.',
          ],
          callouts: [
            callout('Типичная ошибка', 'Смотреть только `mean` и считать, что признак понятен. При выбросах среднее может описывать данные хуже, чем медиана и квантили.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-stats-eda-order',
      'Порядок EDA для признака',
      'Собираем статистики в один рабочий маршрут.',
      [
        section('order', 'От границ к хвостам', [
          'Практичный порядок такой: сначала проверить минимум и максимум, затем сравнить среднее и медиану, затем посмотреть разброс и квантили. Такой маршрут помогает не сделать вывод по одной цифре.',
          'Если максимум сильно больше остальных значений, среднее может быть завышено. Если стандартное отклонение велико, признак может требовать масштабирования или отдельной проверки выбросов.',
        ], {
          callouts: [
            callout('Где используется', 'Первичный анализ числовых признаков, поиск выбросов, выбор стратегии заполнения пропусков, подготовка признаков к масштабированию.', 'example'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-stats-quantiles',
      'Квантили вместо догадок',
      'Показываем, зачем нужны хвосты распределения.',
      [
        section('tails', 'Что происходит на краях', [
          '`quantile` и `percentile` помогают смотреть не только центр распределения, но и края. Например, 0.95-квантиль показывает значение, ниже которого лежит около 95% наблюдений.',
          'Это полезно, когда нужно понять, где начинаются подозрительно большие значения. В отличие от максимума, квантиль не зависит от одного самого экстремального объекта так сильно.',
        ], {
          callouts: [
            callout('Промежуточный вывод', 'Среднее и медиана описывают центр, `std` описывает разброс, а квантили помогают увидеть хвосты.', 'summary'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-aggregations-quiz-outlier',
      'Устойчивость к выбросам',
      'Проверяем выбор статистики.',
      singleQuiz(
        'quiz-numpy-aggregations-outlier',
        'Выбросы',
        'numpy-aggregations-statistics',
        'numpy-ml',
        'В признаке есть сильные выбросы. Какая статистика часто устойчивее среднего?',
        [
          { id: 'a', text: '`median`' },
          { id: 'b', text: '`sum`' },
          { id: 'c', text: '`size`' },
          { id: 'd', text: '`dtype`' },
        ],
        'a',
        'Медиана зависит от середины отсортированного ряда и меньше реагирует на отдельные экстремальные значения.',
      ),
    ),
    quizStep(
      'numpy-aggregations-quiz-spread',
      'Разброс признака',
      'Проверяем инструмент для разброса.',
      singleQuiz(
        'quiz-numpy-aggregations-spread',
        'Разброс',
        'numpy-aggregations-statistics',
        'numpy-ml',
        'Что помогает оценить разброс значений признака?',
        [
          { id: 'a', text: '`std`' },
          { id: 'b', text: '`astype`' },
          { id: 'c', text: '`shape`' },
          { id: 'd', text: '`arange`' },
        ],
        'a',
        '`std` показывает типичный масштаб отклонения значений от среднего.',
      ),
    ),
    practiceStep(
      'numpy-aggregations-practice',
      'Проверка признака на выброс',
      'Сравниваем среднее и медиану.',
      makeStdinTask(
        'task-numpy-mean-median',
        'Проверка признака на выброс',
        'На вход подаются числа одного признака. Выведите среднее и медиану на отдельных строках.',
        `
          import numpy as np

          # TODO: считайте значения

          # TODO: создайте массив

          # TODO: посчитайте среднее и медиану

          # TODO: выведите результаты
        `,
        [
          { id: 's1', description: 'Есть выброс', input: '10 20 30 1000', expectedOutput: '265.0\n25.0' },
        ],
        [
          { id: 'h1', description: 'Без выброса', input: '1 2 3 4', expectedOutput: '2.5\n2.5' },
        ],
        `
          import numpy as np

          values = list(map(float, input().split()))
          x = np.array(values)
          print(np.mean(x))
          print(np.median(x))
        `,
      ),
    ),
  ],
)

export const topicNumpyAxis = numpyTopic(
  'numpy-2d-axis',
  '2.7 Двумерные массивы и axis',
  7,
  'Объясняем `axis=0` и `axis=1` через таблицу объектов и признаков.',
  'В двумерной матрице `axis` задаёт направление вычисления: по столбцам признаков или по строкам объектов.',
  ['axis=0', 'axis=1', 'rows', 'columns', 'features'],
  ['axis=0 -> статистика каждого признака', 'axis=1 -> статистика каждого объекта'],
  [
    '`axis=0` считает вниз по строкам и возвращает результат по столбцам.',
    '`axis=1` считает вправо по столбцам и возвращает результат по строкам.',
    'Для статистики признаков чаще нужен `axis=0`.',
  ],
  [
    theoryStep(
      'numpy-axis-scenario',
      'Матрица как таблица',
      'Связываем `axis` с объектами и признаками.',
      [
        section('meaning', 'Строки - объекты, столбцы - признаки', [
          'В ML двумерный массив часто означает таблицу. Если нужно узнать среднюю площадь, среднее число комнат и среднюю цену, мы считаем статистику **по каждому столбцу**.',
          'Если нужно получить среднее значение признаков внутри каждого объекта, мы считаем **по каждой строке**. Это встречается реже, но помогает понять направление `axis`.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[10, 100],
                            [20, 200],
                            [30, 300]])

              print(X.mean(axis=0))
              print(X.mean(axis=1))
            `, `
              [ 20. 200.]
              [ 55. 110. 165.]
            `, '`axis=0` дал среднее каждого столбца, `axis=1` - среднее каждой строки.'),
          ],
          callouts: [
            callout('Схема', 'axis=0 -> считаем вниз по строкам -> результат по столбцам\naxis=1 -> считаем вправо по столбцам -> результат по строкам', 'schema'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-axis-checkpoint',
      'Что теперь умеем',
      'Закрепляем выбор оси без заучивания.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Вопрос “какой axis выбрать” лучше переводить в вопрос “какой результат мне нужен”.',
        ], {
          bullets: [
            'читать матрицу как объекты × признаки;',
            'считать статистику по признакам через `axis=0`;',
            'считать статистику по объектам через `axis=1`;',
            'проверять форму результата после агрегации.',
          ],
          callouts: [
            callout('Запомнить', 'Если хотим статистику по признакам - чаще нужен `axis=0`. Если хотим статистику по объектам - чаще нужен `axis=1`.', 'summary'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-axis-debug',
      'Как проверить выбранную ось',
      'Даём практический способ не перепутать `axis`.',
      [
        section('debug', 'Смотрите форму результата', [
          'Если в матрице `X` форма `(100, 5)` и вы считаете среднее каждого признака, результат должен иметь длину 5. Если получилась длина 100, значит вы посчитали статистику по объектам, а не по признакам.',
          'Такой способ полезнее заучивания. Перед вычислением сформулируйте ожидаемый результат: “мне нужно одно число на столбец” или “мне нужно одно число на строку”. После вычисления сравните форму результата с ожиданием.',
        ], {
          callouts: [
            callout('Типичная ошибка', 'Выбрать `axis=1` для статистики признаков и получить по одному числу на объект. Код работает, но ответ отвечает на другой вопрос.', 'important'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-2d-axis-quiz-feature',
      'Среднее признаков',
      'Проверяем выбор оси для столбцов.',
      singleQuiz(
        'quiz-numpy-2d-axis-feature',
        'Axis',
        'numpy-2d-axis',
        'numpy-ml',
        'Как посчитать среднее значение каждого признака в матрице `X`?',
        [
          { id: 'a', text: '`X.mean(axis=0)`' },
          { id: 'b', text: '`X.mean(axis=1)`' },
          { id: 'c', text: '`X.mean(dtype=0)`' },
          { id: 'd', text: '`X.shape(axis=0)`' },
        ],
        'a',
        '`axis=0` агрегирует вниз по строкам и возвращает статистику по столбцам.',
      ),
    ),
    quizStep(
      'numpy-2d-axis-quiz-object',
      'Среднее объектов',
      'Проверяем выбор оси для строк.',
      singleQuiz(
        'quiz-numpy-2d-axis-object',
        'Axis по строкам',
        'numpy-2d-axis',
        'numpy-ml',
        'Как посчитать среднее значение по признакам внутри каждого объекта?',
        [
          { id: 'a', text: '`X.mean(axis=1)`' },
          { id: 'b', text: '`X.mean(axis=0)`' },
          { id: 'c', text: '`X[:, 0]`' },
          { id: 'd', text: '`X.astype(float)`' },
        ],
        'a',
        '`axis=1` идёт вправо по столбцам и возвращает результат для каждой строки.',
      ),
    ),
    practiceStep(
      'numpy-2d-axis-practice',
      'Среднее каждого признака',
      'Считаем статистику по столбцам матрицы.',
      makeStdinTask(
        'task-numpy-feature-means',
        'Среднее каждого признака',
        'На вход подаются `n`, `m`, затем `n * m` чисел. Создайте матрицу и выведите среднее каждого столбца.',
        `
          import numpy as np

          # TODO: считайте размеры и значения

          # TODO: создайте матрицу

          # TODO: посчитайте среднее по признакам

          # TODO: выведите результат
        `,
        [
          { id: 's1', description: 'Три объекта и два признака', input: '3 2\n10 100 20 200 30 300', expectedOutput: '[ 20. 200.]' },
        ],
        [
          { id: 'h1', description: 'Два объекта и три признака', input: '2 3\n1 2 3 5 6 7', expectedOutput: '[3. 4. 5.]' },
        ],
        `
          import numpy as np

          n, m = map(int, input().split())
          values = list(map(float, input().split()))
          X = np.array(values).reshape(n, m)
          print(X.mean(axis=0))
        `,
      ),
    ),
  ],
)
