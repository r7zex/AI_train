import { callout, code, makeStdinTask, numpyTopic, practiceStep, quizStep, section, singleQuiz, theoryStep } from '../helpers'

export const topicNumpyIndexing = numpyTopic(
  'numpy-indexing-slices',
  '2.4 Индексация и срезы',
  4,
  'Учимся доставать элементы, диапазоны, строки и столбцы из массива признаков.',
  'Индексация отвечает на практический вопрос: как взять нужные объекты или признаки из массива.',
  ['indexing', 'slicing', 'row', 'column', 'X[:, 0]', 'X[0, :]'],
  ['X[:, 0] -> первый признак для всех объектов', 'X[0, :] -> все признаки первого объекта'],
  [
    'Один индекс достаёт элемент или строку.',
    'Срез достаёт диапазон объектов.',
    'Через запятую выбирают строки и столбцы двумерного массива.',
  ],
  [
    theoryStep(
      'numpy-indexing-scenario',
      'Как достать нужные признаки',
      'Переходим от массива целиком к его частям.',
      [
        section('ml-slices', 'Строки и столбцы в матрице `X`', [
          'После загрузки данных редко работают со всей матрицей сразу. Нужно посмотреть первый объект, взять один признак, выбрать первые 10 объектов или сравнить несколько строк.',
          'В ML удобно читать `X` как таблицу: строки - объекты, столбцы - признаки. Поэтому `X[:, 0]` означает “первый признак для всех объектов”, а `X[0, :]` - “все признаки первого объекта”.',
        ], {
          codeExamples: [
            code('python', `
              import numpy as np

              X = np.array([[35, 1, 8],
                            [52, 2, 9],
                            [80, 3, 18]])

              print(X[0, :])
              print(X[:, 0])
              print(X[:2])
            `, `
              [35  1  8]
              [35 52 80]
              [[35  1  8]
               [52  2  9]]
            `, 'Первый вывод - объект целиком, второй - один признак по всем объектам, третий - первые две строки.'),
          ],
        }),
        section('slice-logic', 'Срезы читаются как диапазоны', [
          '`start:stop` берёт элементы от `start` до `stop`, не включая правую границу. Если граница пропущена, NumPy берёт начало или конец массива.',
          'Эта логика совпадает с Python-списками, но в двумерном массиве срезы можно задавать отдельно для строк и столбцов.',
        ], {
          callouts: [
            callout('Важно', '`X[:10]` берёт первые 10 объектов. `X[:, 0]` берёт первый столбец для всех объектов. Двоеточие означает “все элементы по этой оси”.', 'remember'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-indexing-checkpoint',
      'Что теперь умеем',
      'Закрепляем основные варианты доступа к данным.',
      [
        section('checkpoint', 'Промежуточный вывод', [
          'Индексация нужна не ради синтаксиса, а чтобы быстро собрать нужный фрагмент данных для анализа.',
        ], {
          bullets: [
            'брать один элемент массива;',
            'брать диапазон объектов через срез;',
            'брать строку как один объект;',
            'брать столбец как один признак для всех объектов.',
          ],
          callouts: [
            callout('Типичная ошибка', 'Путать строку и столбец: `X[0, :]` - первая строка, а `X[:, 0]` - первый столбец.', 'important'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-indexing-ml-use',
      'Индексация в EDA',
      'Показываем, зачем доставать части массива в анализе.',
      [
        section('usage', 'Отладка и быстрые проверки', [
          'Индексация нужна в первых шагах анализа: посмотреть первые объекты, проверить один признак, сравнить несколько строк или убедиться, что после преобразования данные не перепутались. Это особенно полезно, когда массив пришёл из pandas или после нормализации.',
          'Например, `X[:5]` помогает увидеть первые объекты, `X[:, 0]` - проверить первый признак, а `X[0, :]` - посмотреть все признаки одного объекта. Эти действия не заменяют полноценный анализ, но быстро отвечают на вопрос “что лежит в массиве?”.',
        ], {
          callouts: [
            callout('Где используется', 'Проверка входа модели, выбор одного признака для графика, сравнение строк до и после масштабирования, отладка формы данных.', 'example'),
          ],
        }),
      ],
    ),
    theoryStep(
      'numpy-indexing-boundaries',
      'Границы срезов',
      'Разбираем правую границу и чтение срезов.',
      [
        section('boundaries', 'Правая граница не входит', [
          'В срезе `start:stop` элемент с индексом `stop` не входит в результат. Поэтому `X[:10]` даёт ровно 10 первых строк: индексы от 0 до 9.',
          'Это правило важно для train/test-разделений в учебных примерах и для любых “первых N объектов”. Ошибка на одну строку редко ломает код сразу, но может незаметно изменить набор данных, который вы анализируете.',
        ], {
          callouts: [
            callout('Запомнить', '`X[a:b]` читается как “начиная с `a` и до `b`, не включая `b`”. Для столбцов правило такое же: `X[:, :2]` берёт первые два признака.', 'remember'),
          ],
        }),
      ],
    ),
    quizStep(
      'numpy-indexing-quiz-feature',
      'Первый признак',
      'Проверяем выбор столбца.',
      singleQuiz(
        'quiz-numpy-indexing-feature',
        'Индексация признаков',
        'numpy-indexing-slices',
        'numpy-ml',
        'Как взять первый признак для всех объектов из двумерного массива `X`?',
        [
          { id: 'a', text: '`X[:, 0]`' },
          { id: 'b', text: '`X[0, :]`' },
          { id: 'c', text: '`X[0, 0]`' },
          { id: 'd', text: '`X[:0]`' },
        ],
        'a',
        'Двоеточие по строкам означает “все объекты”, а `0` по столбцам - первый признак.',
      ),
    ),
    quizStep(
      'numpy-indexing-quiz-objects',
      'Первые объекты',
      'Проверяем срез по строкам.',
      singleQuiz(
        'quiz-numpy-indexing-objects',
        'Срез объектов',
        'numpy-indexing-slices',
        'numpy-ml',
        'Как выбрать первые 10 объектов из `X`?',
        [
          { id: 'a', text: '`X[:10]`' },
          { id: 'b', text: '`X[:, 10]`' },
          { id: 'c', text: '`X[10:]`' },
          { id: 'd', text: '`X[10, :]`' },
        ],
        'a',
        '`X[:10]` берёт строки с индексами от 0 до 9.',
      ),
    ),
    practiceStep(
      'numpy-indexing-practice',
      'Среднее первого признака',
      'Достаём столбец из матрицы и считаем показатель.',
      makeStdinTask(
        'task-numpy-first-feature-mean',
        'Среднее первого признака',
        'На вход подаются `n`, `m`, затем `n * m` чисел. Создайте матрицу `n x m`, возьмите первый столбец и выведите его среднее значение.',
        `
          import numpy as np

          # TODO: считайте n и m

          # TODO: считайте значения и создайте матрицу

          # TODO: выберите первый столбец

          # TODO: посчитайте и выведите среднее
        `,
        [
          { id: 's1', description: 'Три объекта и два признака', input: '3 2\n10 1 20 2 30 3', expectedOutput: '20.0' },
        ],
        [
          { id: 'h1', description: 'Два объекта', input: '2 3\n5 1 1 15 2 2', expectedOutput: '10.0' },
        ],
        `
          import numpy as np

          n, m = map(int, input().split())
          values = list(map(float, input().split()))
          X = np.array(values).reshape(n, m)
          first_feature = X[:, 0]
          print(first_feature.mean())
        `,
      ),
    ),
  ],
)
